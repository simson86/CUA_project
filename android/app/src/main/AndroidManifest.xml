<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>

    <!-- 인앱 음성 인식(SpeechRecognizer)용. 마이크를 우리가 직접 연다.
         마이크 버튼을 '길게 눌러' 쓰는 구글 다이얼로그 경로는 이 권한이 없어도 동작한다
         (그쪽은 구글 앱이 마이크를 열고 우리는 결과 문자열만 받는다). -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>

    <!-- Android 11+ 패키지 가시성: 이 선언이 없으면 다른 앱이 보이지 않아
         queryIntentActivities/getLaunchIntentForPackage 가 빈 결과·null 을 준다.
         런처가 있는 앱만 조회하므로 QUERY_ALL_PACKAGES(민감 권한)보다 안전. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>

        <!-- ★ SpeechRecognizer 가 인식 서비스에 바인딩하려면 이게 있어야 한다.
             없으면 엔진이 멀쩡히 깔려 있어도 isRecognitionAvailable() 이 false 를 준다.
             위 MAIN/LAUNCHER 선언은 이걸 안 덮는다. -->
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Android_run">
        <service
            android:name=".a11service"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="false">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
    </service>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>


</manifest>