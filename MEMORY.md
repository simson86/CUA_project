# MEMORY.md — CU 에이전트 기억 시스템

기억 시스템의 **현재 상태**와 **작업 전에 알아야 할 것**만 담는다.
설계 근거·대안 검토·결정 기록은 전부 아래 명세서에 있다.

> **설계 명세**: `docs/reference/android_run-memory-2026-09-12.html` (브라우저로 열 것)
> 반드시 답해야 하는 아홉 개 질문으로 구성돼 있고, 부록에 스키마·파일구조·결정기록·열린질문이 있다.

**Update rule** — `CLAUDE.md` 의 구현 현황과 같은 규칙이다. 기능이 바뀌면 **같은 커밋에서**
아래를 고쳐라. 줄을 덧붙이는 게 아니라 **기존 줄을 고쳐 쓰는** 문서다.

---

## 한 줄 요약

에이전트가 남긴 일화에서 의미 기억을 뽑아 **기기 안 SQLite** 에 쌓고, 다음 실행에서
상황에 맞는 것만 예산 안에서 모델 입력에 얹는다. **읽기·주입·사람의 편집까지 돈다**
(Unit 1~3). 아직 **자동으로 쓰지는 않는다** — 기억은 사람이 목록 UI 로 넣는 것뿐이고,
실행에서 뽑아내는 리플렉터는 Unit 5 다. 기억이 하나도 없으면 note == null 이라
요청 본문이 종전과 완전히 동일하다.

---

## 구현 현황

| Unit | 무엇 | 상태 |
|---|---|---|
| **1** | **DB 뼈대 + 구조화 로깅** | ✅ **실기기 검증 완료** (2026-09-12, SM-S931N) |
| **2** | **`memory` 테이블 + 읽기 경로** | ✅ **실기기 검증 완료** (2026-09-13) — 주입까지 확인 |
| **3** | **기억 목록 UI (보기·추가·수정·삭제)** | ✅ **실기기 검증 완료** (2026-09-13) |
| **4** | **측정 세트 + 소켓 `DUMP`** | ✅ **산출물 완료** (2026-09-16) → **판단 ① = 조건부** (아래 2차 결과) |
| **6** | **reconciliation + 승격** | 🔶 **승격 카운터 실기기 검증 완료** (2026-09-16, SM-S931N). reconciliation 은 Unit 5 뒤 (아래) |
| **5** | **리플렉터 (추출 + §3 필터)** | 🔶 **5a·5b 실기기 동작 확인** — 5c(reconciliation) 남음 |
| 7 | 랭킹 점수식 | ⬜ |
| 8 | 생애주기 (보존·압축·감쇠·상한·버전태깅) | ⬜ ← **여기까지가 완성** |
| 9 | `RECIPE` (절차 재사용) | ⬜ 선택 |

> **★ 5 와 6 의 순서를 바꿨다.** 원래는 리플렉터(5) → reconciliation(6) 이었다. 2차 측정에서
> **틀린 기억이 에이전트를 적극적으로 망가뜨린다**는 게 확인됐고(+26.7%), 모델이 기억을
> 자기 눈보다 믿는다는 것도 확인됐다. 검증층이 없는 상태로 자동 쓰기를 켜면 **틀린 기억이
> 곧바로 주입된다.** 삭제 UI(Unit 3)를 자동 쓰기보다 먼저 만든 것과 같은 논리다.
>
> **번호는 안 바꿨다** — `5` 를 `6` 으로 고쳐 쓰면 지금까지의 커밋·문서·대화에 남은
> "Unit 5 = 리플렉터"가 전부 어긋난다. 이름은 두고 **표의 순서만** 바꿨다.
>
> ⚠️ **판단 ②의 위치도 따라 옮겨간다.** 원래는 Unit 6 뒤였는데, 6 이 앞으로 오면서
> **6 이 끝나도 리플렉터가 없어 잴 수가 없다** — 자동으로 쌓인 기억이 있어야 종단 측정이
> 성립한다. 실제 순서는 `6(검증층) → 5(리플렉터) → 판단 ②` 다.

**Unit 8 까지가 완성이다.** Unit 9 는 다른 종류의 기억을 얹는 확장이라 없어도 완결된다.
반면 Unit 8 이 없으면 앱이 업데이트될 때마다 낡은 기억이 계속 주입돼 **시간이 지날수록
나빠진다**.

> **소켓 `DUMP` 는 원래 Unit 2 였는데 Unit 4 로 합쳤다.** "쌓인 로그를 사람이 본다"는
> 목적은 **Android Studio 의 Database Inspector** 가 더 잘 해낸다(실시간·GUI·임의 쿼리,
> 만들 것 없음). 소켓 경로에 남는 유일한 쓸모는 **측정 자동화**(`tools/bench_memory.py` 가
> 결과를 프로그램으로 읽는 것)이고, 그건 Unit 4 에서야 필요해진다.

---

## Unit 1 — 지금 코드가 어떻게 생겼나

### 새로 만든 것

```
android/app/src/main/java/com/cua/a11/memory/
├── MemoryDb.kt        run · episode 엔티티 + Room DB (version 1)
├── MemoryDao.kt       INSERT + 검증/집계 쿼리
└── RoomRunTrace.kt    RunTrace 구현 — 모든 실패를 삼킨다
```

### 기존 파일에서 바뀐 것 (세 군데뿐)

| 위치 | 무엇 |
|---|---|
| `CuClient.kt` | `Executor.foregroundApp()` · `RunTrace`/`TurnRecord` 계약 · `runAgent(trace=null)` 훅 |
| `a11service.kt` | `foregroundApp()` 구현 · `runTrace` lazy · 앱/소켓 두 경로에 주입 |
| `android/gradle.properties` 외 | Room·KSP 빌드 설정 (아래 Gotchas) |

### 경계

`runAgent` 는 **DB 도 Room 도 모른다.** `RunTrace` 인터페이스만 알고 기본값이 `null` 이라,
안 넘기면 동작도 요청 본문도 종전과 완전히 동일하다. 이미 있는 `log`/`cancel` 과 같은 패턴.

### 담는 것 / 안 담는 것

- **좌표는 안 담는다**(설계 원칙 1). `action` 이름과 `intent` 만 남긴다.
- **`longVersionCode` 는 지금 안 쓰지만 기록한다.** 무효화의 근거인데(설계 §7 4번) 안 적어두면
  나중에 소급이 안 된다.
- **`hadSafety`** = `require_confirmation` 이 붙은 턴 표시. 그 턴에서 나온 후보를 통째로
  버리는 데 쓴다(설계 §3 규칙 A) — 내용 판정 없이 **출처로 자르는** 방식.

---

## Unit 2 — memory 테이블과 읽기 경로

### 새로 만든 것
`memory/MemoryGateway.kt` — **DB 를 만지는 유일한 출입구.** 예산(APP_FACT 2줄 + PITFALL 2줄)·
민감도 필터·우리 앱 제외가 전부 여기 있다. 호출부에서 다시 걸지 말 것.

### 기존 파일에서 바뀐 것
- `CuClient.kt` — **`taskNote` 를 `CuClient` 에서 `Executor` 로 옮겼다.** `appNote` 는 이미
  거기 있었는데 `taskNote` 만 판단 코어 안에 있었다. 이제 두 노트가 **같은 경계**를 지나고
  `CuClient` 는 기억을 완전히 모른다.
- `a11service.kt` — 하드코딩 맵 두 개를 지우고 `gateway.readForApp/readForTask` 로.

### 아직 안 만든 것 (일부러)
**랭킹.** 기억이 몇 개일 때 점수식은 무의미하다 — 하드 필터 + `LIMIT` 뿐이고 점수식은
Unit 7 이다. `PITFALL` 키워드 매칭도 FTS5 대신 **코틀린에서** 한다(목표 문장을 어떻게
쪼갤지가 아직 안 정해졌다).

### 검증된 것 / 안 된 것
| | |
|---|---|
| 스키마 1→2 마이그레이션 | ✅ 기존 데이터 생존(run 4행·episode 9행) |
| `memory` 22칸 + 인덱스 2개 | ✅ |
| 손으로 쓴 DDL 이 Room 기대치와 일치 | ✅ `app/schemas/2.json` 과 대조 |
| `note=null` 일 때 요청 본문 종전 동일 | ✅ |
| **`memory` 행이 실제로 주입되는지** | ✅ **Unit 3 에서 확인** (아래) |

## Unit 3 — 기억 목록 UI

### 새로 만든 것
```
memory/MemoryActivity.kt        목록 · 편집 다이얼로그 · 삭제
res/layout/activity_memory.xml  목록 화면
res/layout/item_memory.xml      행 하나
res/layout/dialog_memory_edit.xml  편집 폼
```
`MemoryDao` 에 `updateMemory`/`deleteMemory`/`deleteAllMemories`,
`MemoryGateway` 에 `list`/`save`/`delete`/`deleteAll`/`validate` 를 더했다.
`MainActivity` 에 `기억 관리` 버튼 하나.

### 게이트웨이의 읽기·쓰기 비대칭 ★
**읽기는 실패를 삼키고 쓰기는 안 삼킨다.** 일부러 다르게 뒀다 —
에이전트 입장에선 "기억이 없다"와 "DB 가 깨졌다"가 같은 결과지만(둘 다 note == null 로
종전대로 돈다), 사람이 저장을 눌렀는데 조용히 실패하면 **저장된 줄 알고 화면을 뜬다.**
`MemoryActivity.io()` 가 예외를 받아 토스트로 띄운다.

### 저장을 막는 것 = 읽기에 절대 안 걸리는 행
`MemoryGateway.validate()` 가 네 가지를 거절한다: 빈 내용 · 패키지 없는 `APP_FACT` ·
`pkg == com.cua.a11` · 키워드 없는 `PITFALL`. 전부 **저장은 되는데 아무 때도 안 나오는**
행이다. 그런 게 목록에 쌓이면 사용자는 "기억 기능이 고장 났다"고 결론 내린다.
⚠️ **읽기 조건(`activeForApp`/`readForTask`)을 바꾸면 `validate()` 도 같이 고쳐야 한다.**

### `RECIPE` 는 종류 드롭다운에서 뺐다
읽는 쪽(Unit 9)이 아직 없다. 넣을 수 있게 두면 사용자가 **아무도 안 읽는 행**을 만든다.
Unit 9 에서 `MemoryGateway.KINDS` 에 한 줄 더하면 열린다.

### 상태 드롭다운 — 새로 만들 때와 고칠 때의 요구가 정반대다 ★
| | 상태를 고를 수 있나 | 왜 |
|---|---|---|
| **새 기억** | ❌ `ACTIVE` 고정(비활성) | 명세 §7 이 **사람의 판단 = 즉시 ACTIVE**. `PENDING` 은 *기계 출력*을 담는 검역이라 사람이 쓴 문장을 넣을 이유가 없고, 지금은 나가는 문도 없다(승격 = reconciliation) |
| **고칠 때** | ✅ 세 상태 전부 | 리플렉터가 쌓은 `PENDING` 후보를 사람이 보고 올리는 것이 설계 §8 의 '아키텍처의 일부' |

⚠️ **`MemoryGateway.STATES` 자체를 줄이는 방식으로 풀지 말 것.** 줄이면
`fill()` 의 `indexOf(...).coerceAtLeast(0)` 이 목록에 없는 값을 **0번(`ACTIVE`)** 으로
되돌려, 사람이 `PENDING` 후보를 **열었다 저장하는 것만으로 검역이 조용히 풀린다.**
게다가 "Unit 5 전에 되돌리기"라는 숙제가 남는데, 이 저장소는 그런 걸 실제로 잃어버린 적이
있다(Stop 훅). **되돌릴 게 없는 쪽**을 골랐다 — 선택지를 좁히는 일은 다이얼로그의
`existing == null` 분기 안에서만 한다.

**목록은 아무것도 거르지 않는다**(`SELECT * FROM memory`). 리플렉터가 만든 `PENDING` 도
그대로 뜬다 — 그게 §8 의 목적이다. **§7 의 카운터로는 모델의 체계적 편향을 못 막는다**:
같은 모델이 같은 편향을 가지면 서로 다른 실행에서도 똑같이 틀리고, 재관측 카운터는 그걸
오히려 승격시킨다. 사람 눈이 유일한 방어선이다.

### 주입이 로그에 보이게 했다
`runAgent` 가 `[기억] …` 을 찍는다 — **note 가 바뀔 때만.** 요청 본문은 폰에서 볼 수 없어
이 줄이 없으면 주입 여부를 확인할 방법이 없고, 매 턴 찍으면 같은 문장이 로그를 덮는다.

⚠️ **찍는 것은 반드시 '실제로 보낸 것'이어야 한다.** 승인 턴은 `result` 가 객체라
`putResult` 가 note 를 안 붙이는데, 로그를 그 앞에서 무심코 찍으면 **안 실린 걸 찍는다**.
이 로그의 존재 이유가 신뢰이므로 거짓말을 하면 있느니만 못하다 — `logNote` 에는
`if (safetyAck) null else turnNote` 를 넘긴다.

### 실패를 드러내는 세 갈래 (Unit 3 뒤에 덧붙임)
읽기 실패를 삼키는 건 유지하되, **아무도 모르는 상태**는 없앴다. 자세한 건 아래 Gotchas 의
`memoryReadFailed` 항목. 코드로는 세 곳이 바뀌었다:

- `MemoryGateway` — `guard` 의 catch 에서 깃발 두 개를 세운다(로그용·run 행용, 소비 시점이
  다르다). `takeFailureForLog()` / `takeFailureForRun()` 으로 **한 번 가져가면 사라진다.**
- `Executor.noteFailure()` — 새 계약. `runAgent` 는 기억을 모르므로 이 통로로만 알 수 있다.
  실행 로그에 `⚠ [기억] 읽기 실패…` 로 뜬다.
- `RoomRunTrace.onRunEnd` — 깃발을 `run.memoryReadFailed` 에 기록.
  **시작이 아니라 종료에서 소비한다** — 시작에서 지우면 `runAgent` 가 `onRunStart` 보다
  **먼저** 부르는 `taskNote` 의 실패가 지워진다.

목록 UI 에도 `lastAccessed` 를 노출했다 — `주입 N회 · 마지막 …`, 0회면
`아직 한 번도 안 걸림 — 검색 키를 확인해 보세요`, 요약줄에 `그중 n건은 아직 안 걸림`.
이건 **예외가 안 나는 실패**(키워드가 안 맞아 그냥 조회가 0건)의 유일한 신호다. 다만
"검색 키가 틀렸다"와 "그 상황이 아직 안 왔다"를 구분하지는 못한다 — 판정이 아니라 신호다.

### 실기기 검증 (2026-09-13, SM-S931N)
`APP_FACT / com.android.settings / "Use the search icon at the top to find a setting quickly."`
를 UI 로 넣고 `Open the Settings app` 을 실행:

```
[턴 1] list_apps                        ← pkg=com.cua.a11, note 없음
[턴 2] open_app com.android.settings
[기억] Use the search icon at the top…  ← 주입됨
[완료] The Settings app has been opened.
```
| | |
|---|---|
| `memory` 행이 실제로 주입되는지 (Unit 2 이월) | ✅ `episode.turn=2` 의 `note` 에 문장 그대로 |
| `numRecalled` 증가 · `lastAccessed` 기록 | ✅ 0 → 1 |
| 턴 1 은 note 없음(우리 앱이 포그라운드) | ✅ 설계대로 |
| 추가 · 편집 · 삭제 | ✅ |
| 편집이 이력을 안 지우는가 (`copy()`) | ✅ `numRecalled=1`·`timeAdded` 보존 |
| 종류 바꾸면 검색 키 칸이 바뀌는가 | ✅ 패키지 ↔ 키워드 |
| 빈 내용 저장 거절 + **다이얼로그 유지** | ✅ |
| 삭제가 `run`·`episode` 를 안 건드리는가 | ✅ 6 run · 20 episode 그대로 |
| `a11mem` 경고 | ✅ 없음 |
| 스키마 2→3 마이그레이션 (`memoryReadFailed`) | ✅ 데이터 생존(run 6·episode 20·memory 1), 옛 행은 `null` |
| 정상 실행에 `memoryReadFailed=0` 기록 | ✅ |
| **읽기 실패 경로 자체** | ⬜ **미검증** — 아래 |

⚠️ **실패를 일부러 일으켜 보지는 못했다.** 확인된 것은 **배선**이다 —
`onRunEnd` 가 `gateway.takeFailureForRun()` 의 값을 실제로 `run` 행에 썼다(`0`). 안 밟아 본
것은 `guard` 의 catch 안 두 줄(깃발 대입)뿐이다. 다음에 스키마를 바꿀 때가 자연스러운
기회다 — 그때 마이그레이션을 빼먹으면 이 경로가 실제로 돈다.

**`PITFALL` 경로도 확인** (같은 날). `APP_FACT` 와 **다른 함수**를 타고(`readForTask`)
매칭도 SQL 이 아니라 코틀린 문자열 `contains` 라 따로 봐야 했다. 키워드 `settings` 짜리
행을 넣고 `Open the Settings app` 실행:

```
[기억] PITFALL-TEST: …                      ← 턴 1 앞. taskNote 는 첫 요청부터 붙는다
[턴 1] list_apps
[턴 2] open_app com.android.settings
[기억] PITFALL-TEST: … Use the search icon…  ← taskNote + appNote 가 합쳐짐
```
`episode.turn=1` 의 `note` 에 PITFALL 만, `turn=2` 에 둘 다 들어 있었다.
**두 노트가 합쳐지는 것까지 이걸로 확인됐다** — 전엔 한 갈래씩만 봤다.

⚠️ 확인한 건 **키워드가 목표에 그대로 들어 있을 때** 걸린다는 것뿐이다. 매칭이 부분
문자열이라 `"휴지통에 넣어줘"` 는 `삭제` 에 안 걸린다 — 그 취약성은 그대로다(설계 §5).

## Gotchas — 기억 시스템 특유의 것

### 소켓 실행은 '무인 실행'이다 — 사람을 기다리는 지점이 함정 ★
**해결됨 (2026-09-13).** 소켓 `RUN` 이 턴 2 에서 6분 22초 멈추던 원인은 Doze 도 네트워크도
아니었다. **인계 카드가 없는 사람을 기다린 것**이다.

```
화면이 꺼짐 → 캡처가 25KB 검은 PNG → probeSecureScreen ≥95% → '보안 화면' 판정
            → 인계 카드 표시 → 아무도 안 누름 → HANDOVER_TIMEOUT_MIN(3분) 소진
```
재현(2026-09-13): `18:00:00 cmd RUN` → `18:03:07 [턴 1]` → `18:03:24 [완료]`.
**203초 중 187초가 대기.** 원래 본 6분 22초는 이게 두 번 걸린 것이다.

**핵심은 꺼진 화면과 보안 화면이 픽셀로 똑같이 검다는 것.** 사람이 앞에 있으면 이 혼동이
무해하다 — 탭 한 번이면 된다. 무인 실행에서만 비용이 된다.

⚠️ 커밋 `a89f9ed` 의 "Doze 로 죽는다"는 **틀렸다.** `readTimeout` 60초로 6분 22초가 설명이
안 됐던 게 단서였는데 그때 더 안 팠다.

**고친 것** (`a11service.kt`):

| | |
|---|---|
| `PowerManager.isInteractive()` 로 꺼진 화면과 보안 화면을 구분 | 꺼져 있으면 즉시 실패. 깨워 봐야 잠금화면이라 방법이 없다 |
| `attended` 깃발 — 앱 UI `true`, 소켓 `false` | 무인이면 인계·자격증명 카드를 아예 안 띄우고 즉시 실패 |
| 소켓 `RUN` 에 `cancel = { cancelled }` | 아래 별도 항목 |
| 진행 표시 띠에 `FLAG_KEEP_SCREEN_ON` | 앱 UI 실행 중에는 화면이 안 꺼진다 |
| `latch.await()` 세 곳에 상한 | 아래 별도 항목 |

**실기기 검증 (2026-09-13, SM-S931N)**

| | |
|---|---|
| 소켓 · 화면 꺼짐 | ✅ **187초 → 0.4초**, `"오류: 화면이 꺼져 있습니다…"` 를 돌려줌 |
| 소켓 · 잠금화면 | ✅ 0.9초, `pkg=com.android.systemui` 까지 짚음 |
| 소켓 · 정상 실행 | ✅ 37.5초 완주 — 회귀 없음 |
| 소켓 · 실패 직후 서버 | ✅ `SHOT` 0.3초 — 죽지 않는다 |
| 앱 UI · 진행 표시 띠 | ✅ 그대로 뜸, 실행 완주(2턴 ~21초) |
| 앱 UI · `KEEP_SCREEN_ON` | ✅ 창 플래그 `fl=10000b8` → `0xb8 = 0x80│0x20│0x10│0x08` |
| 앱 UI · 실행 후 정리 | ✅ 오버레이 0개 — 플래그가 안 남는다(화면이 다시 꺼질 수 있다) |

창 플래그는 `adb shell dumpsys window windows | grep -A6 com.cua.a11` 의 `fl=` 로 본다.
`0x80` 이 `FLAG_KEEP_SCREEN_ON` 이고 나머지는 종전 셋(`NOT_FOCUSABLE`·`NOT_TOUCHABLE`·
`NOT_TOUCH_MODAL`)이다 — **띠의 터치 통과 정책은 그대로**여야 한다(안 그러면 최상단 탭을
가로챈다, `CLAUDE.md` 참조).

### 소켓 `RUN` 은 `cancel` 을 안 넘기고 있었다 ★
```kotlin
runAgent(this, cu, task, trace = runTrace)   // cancel 인자 없음 → 기본값 {false}
```
그래서 **소켓 실행은 중단이 아예 안 됐다** — 앱의 중단 버튼도, 인계 타임아웃이 세우는
`requestCancel()` 도 무시됐다. 위 재현에서 인계가 3분 만에 포기한 뒤에도 검은 화면을 들고
그대로 진행한 이유다. `cancelled`·`skipBlackPkgs` 초기화도 빠져 있어 함께 넣었다
(안 하면 지난 실행의 `cancelled=true` 를 물려받아 첫 턴에서 죽는다).

**새 실행 경로를 만들 때 `runTask` 가 하는 초기화를 그대로 따라 할 것** —
`cancelled` · `skipBlackPkgs` · `attended` · `cu.configure`.

### 콜백 대기에 상한이 없으면 소켓 서버가 통째로 죽는다 ★
소켓 서버는 **단일 스레드**다 — `accept()` 루프에서 `runAgent` 를 그대로 돌린다. 그래서 그
스레드가 멈추면 포트는 `LISTEN` 인데 **아무 명령도 안 받는다.** 실제로 그 상태를 봤다:
TCP 핸드셰이크는 되는데 268초 동안 앱 로그가 0줄(`client connected` 조차 없음).

상한 없이 콜백을 기다리던 곳이 셋이었다 — 전부 상한을 뒀다:

| 위치 | 기다리는 것 | 상한 | 시간 초과 시 |
|---|---|---|---|
| `capturePngBlocking` | `takeScreenshot` 콜백 | 10초 | 던짐 (정상값 ~0.3초) |
| `dispatchBlocking` | `dispatchGesture` 콜백 | 10초 | 던짐 → 모델에 `status:error` |
| `hideForShot` | 메인 스레드 `ui.post` | 5초 | 로그만 남기고 진행 |

⚠️ **새로 `latch.await()` 를 쓸 때 반드시 상한을 둘 것.** 이 코드베이스에서 무한 대기는
'그 요청이 느려지는' 문제가 아니라 **'서버가 재시작 전까지 죽는'** 문제다.

### `fail` 실행을 싸게 만드는 법 — 앱 UI `maxTurns=1` + PITFALL ★
소켓 `RUN` 은 **최대 턴이 20 고정**이라 실패를 만들려면 20턴을 다 태워야 한다(수 분 + API 비용).
앱 UI 는 1~40 을 받으므로 **`maxTurns=1` 로 두면 15초짜리 `fail` 이 나온다.**

⚠️ 이때 **`APP_FACT` 를 쓰면 안 된다.** 앱 UI 로 실행하면 턴 1 의 포그라운드가 우리 앱
(`com.cua.a11`)이라 **주입 대상에서 제외**된다 — 주입이 없으면 점수도 안 움직여서 아무것도
못 잰다. `PITFALL` 은 `taskNote` 라 **첫 요청부터** 붙으므로 1턴 실행에서도 확실히 주입된다.

adb 로 UI 를 몰 수 있다(`input tap` + `input text`). 한글은 `input text` 로 안 들어가니
목표를 영어로 쓰고 키워드에 영어를 넣을 것.

⚠️ **`max_turns` 는 '실행'을 눌러야 저장된다.** 입력칸에 20 을 적어 놔도 `putInt` 가
실행 버튼 핸들러 안에 있어서, 그대로 두고 나가면 **저장값은 1 그대로**다. 다음에 앱을
열면 1 이 다시 채워져 **모든 실행이 1턴 만에 죽는다.** 측정이 끝나면 값을 되돌리고
**실행을 한 번 눌러 저장까지** 시킬 것.

### 앱을 force-stop 하면 접근성 서비스가 꺼진다
`adb shell am force-stop com.cua.a11` 을 쓰면 안드로이드가 그 앱의 접근성 서비스를
**비활성화**한다. 다시 켜려면 사람이 설정에서 켜야 한다(접근성 권한이라 adb 로 되돌리는 것은
하지 않는다). 재테스트할 때 force-stop 대신 **앱 UI 에서 다시 실행**하는 편이 낫다.

### DB 실패는 조용하다 — 그리고 **크래시로 알 수 없다** ★
`RoomRunTrace` 가 모든 **쓰기** 실패를, `MemoryGateway.guard` 가 모든 **읽기** 실패를
삼킨다. 기억이 실행을 망가뜨리면 안 되므로 의도한 것이지만, 대가가 크다.

⚠️ **`MemoryDb` 를 만지는 경로가 셋인데 셋 다 예외를 잡는다** —
`RoomRunTrace.swallow` · `MemoryGateway.guard` · `MemoryActivity.io`.
그래서 **마이그레이션이 어긋나도 앱은 안 죽는다.** Room 은 `build()` 가 아니라 **첫 쿼리**에서
DB 를 여는데, 그 첫 쿼리가 하필 `onRunStart`(= `swallow` 안)다. 실제로 일어나는 일은:

| | |
|---|---|
| 앱·에이전트 | **완벽히 정상으로 보인다** |
| `run`·`episode` | 한 줄도 안 쌓인다 |
| 기억 주입 | 하나도 안 된다 |
| 사람이 볼 수 있는 단서 | `기억 관리` 화면의 토스트, 그리고 logcat `a11mem` |

앱이 멀쩡히 도니 DB 를 의심할 이유가 없어서 **원인을 찾는 데 오래 걸린다.** 스키마를 바꾼
직후에는 반드시 아래 둘 중 하나로 확인할 것:
```bash
adb logcat -s a11mem:W AndroidRuntime:E     # 조용하면 통과
```
또는 `기억 관리` 화면을 열어 본다(거기만 예외를 사람에게 보여준다).

**연쇄 실패 주의**: `onRunStart` 가 실패하면 `run` 행이 없고 → `episode` INSERT 가 외래키
위반 → 그것도 삼켜진다 → **그 실행의 episode 가 전부 조용히 사라진다.** "행이 0개"와
"안 들여다본 것"을 구분하려면 **반드시 DB 를 직접 확인해야 한다.**

### 삼키는 것과 숨기는 것은 다르다 — `memoryReadFailed` ★
위의 조용함이 **측정을 오염시킨다.** 읽기가 실패한 실행이 "기억 있음" 조건에 섞이면 Unit 4 는
그걸 *"기억은 효과가 없다"* 로 읽는다. `CLAUDE.md` 의 PowerShell 인코딩 버그와 같은 구도다 —
**오염된 측정은 조용히 틀린다.**

그래서 삼키되 흔적은 세 군데 남긴다:

| 어디에 | 무엇 | 누가 본다 |
|---|---|---|
| logcat `a11mem` | 예외 전문 | 개발자 |
| 실행 로그 `⚠ [기억] 읽기 실패…` | 한 줄 | **폰만 쓰는 사용자** |
| `run.memoryReadFailed` | `1` | **Unit 4 의 측정 스크립트** |

`null` = 이 칸 이전의 실행(모름) · `0` = 정상 · `1` = 실패. 오염된 실행은
`MemoryDao.contaminatedRuns()` 로 뽑는다.

⚠️ **이 깃발은 "DB 는 살아 있는데 읽기가 실패한" 경우만 잡는다.** DB 가 통째로 죽으면
`onRunStart` 부터 실패해 **`run` 행 자체가 없다** — 측정에서 기대 건수와 실제 건수를
대조해야 알아챈다.

**실패 원인은 거의 다 '가끔'이 아니라 '항상'이다** — 마이그레이션 불일치, 메인 스레드 호출.
즉 확률적으로 삐끗하는 게 아니라 **켜져 있거나 통째로 죽어 있거나** 둘 중 하나다. 나쁜 쪽은
한 번 죽으면 그날 측정이 전부 오염된다는 것이고, 좋은 쪽은 **깃발 하나로 잡힌다**는 것이다.

### 턴 1 의 패키지는 우리 앱 자신이다 ★
실측(2026-09-12): 턴 1 의 `pkg` 가 `com.cua.a11` 이었다. 사용자가 CUA 앱에서 실행을 누르니
포그라운드가 우리 앱이고 `list_apps` 는 화면을 안 바꾸기 때문이다.

- **기록은 그대로 둔다** — 사실이고, "몇 턴 만에 우리 앱을 벗어났나"라는 신호도 된다.
- **Unit 5 의 리플렉터가 `com.cua.a11` 후보를 만들지 않아야 한다.** 우리 앱은 에이전트가
  *조작하는* 앱이 아니라 *실행이 시작된 곳*이라 앱 지식이 될 수 없다.
- 런처(`com.sec.android.app.launcher`)는 **거르지 말 것** — "앱 서랍은 위로 스와이프" 같은
  건 진짜 앱 지식이다. 제외 대상은 우리 앱 하나뿐이다.
- 부수 효과: 설계 부록 F의 **Q7**("턴 1 에 `appNote` 를 붙일까")이 "붙일 앱 지식이 애초에
  없다" 쪽으로 기운다. 표본 1 이라 확정은 아니다.

### AGP 9 내장 Kotlin × KSP — 해제 플래그가 필요하다 ★
이 프로젝트는 Kotlin 플러그인을 따로 선언하지 않는다(AGP 9.2.1 의 **내장 Kotlin**). 그런데
KSP 는 생성 소스를 `kotlin.sourceSets` 로 등록하고, 내장 Kotlin 은 그걸 막는다:

```
Using kotlin.sourceSets DSL to add Kotlin sources is not allowed with built-in Kotlin.
```

AGP 가 에러 메시지로 안내하는 공식 플래그를 `android/gradle.properties` 에 넣어 풀었다:
```properties
android.disallowKotlinSourceSets=false
```
**이 줄을 지우면 `:app:compileDebugKotlin` 이 죽는다.** KSP 버전은 Kotlin 2.2.10 에 맞춰
`2.2.10-2.0.2`.

### `action` 은 SQLite 예약어다
`SELECT pkg AS action ...` 이 파싱 실패한다(`mismatched input 'action'`). 컬럼명으로는
괜찮은데 **`AS` 별칭으로는 안 된다.** Room 이 이걸 빌드 시점에 잡아줬다 — 생
`SQLiteOpenHelper` 였으면 그 쿼리를 처음 부르는 런타임에 터졌을 것이다.

### `.db` 는 루팅 없이 못 본다 — WAL 까지 같이 꺼낼 것
```
/data/data/com.cua.a11/databases/memory.db      ← 본체
                                 memory.db-wal  ← 최근 쓰기가 여기 있다
                                 memory.db-shm
```
디버그 빌드라 `run-as` 로 꺼낼 수 있다. **바이너리이므로 `adb shell` 이 아니라 `exec-out`**
을 써야 깨지지 않는다:
```bash
for f in memory.db memory.db-wal memory.db-shm; do
  adb exec-out run-as com.cua.a11 cat "databases/$f" > "$f"
done
```
본체가 4KB 인데 `-wal` 이 80KB 인 건 정상이다(아직 체크포인트 전). 세 파일을 같이 가져와야
내용이 보인다. **기기에 `sqlite3` 바이너리는 없다** — PC 로 꺼내 읽어야 한다.

**하지만 보통은 이럴 필요가 없다** — `Android Studio → View → Tool Windows →
App Inspection → Database Inspector` 가 기기에서 도는 DB 를 실시간 표로 보여주고 임의
쿼리도 돌려준다. WAL 도 exec-out 도 신경 쓸 필요 없다. 위 절차는 Android Studio 를 못
쓰는 상황의 대안이다.

---

## 열린 결정

숫자는 전부 **근거 없이 정했거나 미정**이다. 스펙처럼 읽지 말 것. 전체 목록은 명세서 부록 F.

| | 항목 | 언제 정하나 |
|---|---|---|
| **N1** | 랭킹 가중치 `0.40/0.25/0.15/0.20` | Unit 7, 측정으로 |
| **N2** | `kind` 별 감쇠 반감기 | Unit 8, `pkgVersion` 변경 간격에서 역산 |
| **N3** | 용량 상한 500건 | Unit 8, 실제 축적량 보고 |
| **N4** | 보존 기간 30/90/180일/1년 | Unit 8 |
| ~~N5~~ | ~~`score − 1` 가중치~~ | ✅ **닫힘 (2026-09-16)** — 아래 Unit 6 절 |
| **N6** | `Recent:` 개수 3 | Unit 2 |
| **Q7** | 턴 1 에 `appNote` 를 붙일지 | 🔶 **'안 붙인다' 쪽으로 기움** — 아래 |
| **Q13** | **정확도를 어떻게 잴 것인가** | 🔴 **설계 없음** — 아래. 지금까지 잰 것은 전부 턴 수뿐이다 |
| **Q11** | ~~Room 이냐 생 SQLiteOpenHelper 냐~~ | ✅ **Room 채택** (2026-09-12) |

`MemoryDao` 에 **Q7·N2 를 답할 쿼리를 미리 넣어뒀다** — `firstTurnActions()`,
`versionSpread()`. Database Inspector 에서 바로 돌려볼 수 있다.

**Q7 중간 결과** (2026-09-13, `firstTurnActions()`, N=6):

```
턴 1 액션 : list_apps 6/6
턴 1 패키지: com.cua.a11 4 · com.sec.android.app.launcher 1 · com.kakao.talk 1
```

턴 1 은 **예외 없이 `list_apps`** 였다. 앱 지식이 필요 없는 액션이므로 붙여 봐야 쓸 데가
없다. 흥미로운 건 두 번째 줄 — 포그라운드가 늘 우리 앱인 건 **아니다**(소켓 실행은
사용자가 다른 앱에 있을 때 시작된다). 그런 경우에도 액션은 `list_apps` 였다.
**N=6 이라 확정은 아니다** — Unit 4 에서 표본이 늘면 다시 볼 것.

---

## 템플릿 형식 측정 — **경로가 효과의 전부였다** (2026-09-16) ★

리플렉터(5b)를 만들기 전에 잰 것. 리플렉터의 출력은 자유 문장이 아니라
`trigger → consequence` 템플릿인데, **그 형식이 성능을 깎으면 리플렉터는 태생적으로
약한 문장만 만든다.** 다 만든 뒤에는 "추출이 나쁜 건지 형식이 나쁜 건지" 구분할 수 없다.

과제는 mega 하나(2차에서 −48.8% 로 유일하게 큰 효과), `3.5-flash/low`, 4조건 × 5회 번갈아.
원자료: `docs/bench/template_form.jsonl`

| 조건 | 무엇 | 평균 | 낱값 | vs A |
|---|---|---|---|---|
| **A** | 기억 없음 | 16.5 | 15,19,14,18 | — |
| **B** | 원문(자유 문장 1건) | **8.8** | 8,9,10,8 | **−46.7%** |
| **C** | 같은 내용을 템플릿꼴 2건 | 10.2 | 11,11,9,10,10 | −38.2% |
| **D** | 부정 사실(ⓑ)만 1건 | **18.0** | 18,20,17,17,18 | **+9.1%** |

**사전 기준(20%)대로의 판정:** 형식 비용 `C vs B = +16.6%` → **문턱 미달**.
경로의 기여 `D vs C = +76.5%` → **경로(ⓐ)가 실질 기여**.

### ① 2차가 재현됐다
`B = 8.8` 로 **2차와 소수점까지 같다**(A 는 17.2 → 16.5). 이 과제·이 문장의 효과는
일회성이 아니다. 이 프로젝트에서 재현이 확인된 첫 측정이다.

### ② 부정 사실만으로는 **아무것도 안 된다** ★★
D 는 **전혀 줄이지 못했다**(18.0 vs 기억 없는 A 16.5).

⚠️ **"오히려 해롭다"고 쓰면 안 된다.** `+9.1%` 는 우리 문턱(20%) 아래고 범위도 겹친다
(A 14~19, D 17~20). D 의 `20` 은 `fail` 이라 **절단값**이라서, 빼면 17.5(`+6.1%`)로 더
약해진다. 확실한 것은 **대비**다 — 같은 배치에서 B −46.7%, C −38.2% 인데 D 만 0 이다.
그 차이는 겹치지도 않는다.

내가 틀렸던 부분이다 — mega 기억의 두 문장 중 **뒷문장(`검색해도 커리큘럼은 안 나온다`)이
효과의 몫**이라고 봤다. "검색으로 헤매던 9~10턴을 없앤 게 −49%" 라는 2차 해석 때문이었는데,
**"검색하지 마라"만으로는 어디로 갈지 모르니 다른 데서 헤맨다.**

> **함정을 알려주는 것과 길을 알려주는 것은 다르다.** 전자만으로는 턴이 안 줄었다.

⚠️ **이 측정은 `PITFALL` 종류를 잰 적이 없다.** 네 조건 전부 `kind: APP_FACT` 였다.
`PITFALL` 은 *내용이 부정적이라서* 가 아니라 **검색 키(목표 키워드)와 주입 경로
(`taskNote`)** 로 정의되는 종류다. 잰 것은 **'부정 내용'** 이지 종류가 아니다.

다만 함의는 있다 — `PITFALL` 을 만든 목적이 바로 그런 내용을 담는 것이다.
**`PITFALL` 종류의 효과는 지금까지 한 번도 측정된 적이 없다**(Unit 3 에서 주입되는 것만
확인했다).

**진짜 축은 부정/긍정이 아니라 `방향이 있는가` 다.** 성공한 B·C 도 서술문이지만
*어디로 가면 닿는지* 를 말한다. D 는 서술문이면서 **갈 곳이 없다.** 그래서 이 결과는
*"기억은 '이렇게 해라'가 아니라 '이렇게 생겼다'로 쓴다"* 는 기존 규칙과 충돌하지 않는다 —
**서술문이면서 방향이 있는 것이 가능하고, 그게 통한다.**

**한 겹 더 — 값어치는 "후보를 몇 개나 지우느냐" 다.** 커리큘럼이 있을 만한 곳이
대여섯 군데(검색·좌상단 메뉴·홈 배너·하단 탭·마이페이지…)라고 보면:

| 기억 | 남는 후보 |
|---|---|
| *"검색엔 없다"* (D) | **다섯** — 여전히 뒤져야 한다 |
| *"메뉴 → 목록 → 과목 → 선생님"* (B·C) | **하나** |

그래서 *"부정 정보는 쓸모없다"* 가 아니라 **"부정 정보의 값어치는 그것이 지우는 후보
수에 비례한다"** 가 더 정확하다. **이건 재볼 수 있는 주장이다** — 후보가 둘뿐인 화면에서는
"거기 없다"가 "여기 있다"와 같아진다.

⚠️ **사람 비유가 깨지는 지점**: 사람은 *"거기 없어"* 를 들으면 **되묻는다**. 에이전트는
안 되묻는다 — `request_user_input` 은 *모르는 **값***(인증번호·아이디)을 받는 통로지
*"어디로 가야 하나"* 를 묻는 통로가 아니다. 실측에서도 모델은 막히면 사람에게 묻는 대신
**자기가 앱을 뒤지러 갔다**(`list_apps`/`open_app`). 부정 정보만 주는 것은
**"거기 없어"라고 말하고 자리를 뜨는 것**에 가깝고, 그 공백을 메울 사람이 없다.

→ **5b 프롬프트에 그대로 쓴다.** *"함정을 찾아라"* 가 아니라
*"이 실행에서 후보를 가장 많이 지운 발견이 무엇이었나"* 로 물을 것.

### ⚠️ 그런데 이 축은 **턴 수** 이야기다 — 값의 절반만 본 것이다 (Q13) ★★
*"의미형은 효과가 없다"* 는 결론은 **지표가 턴 수일 때만** 참이다. `MEMORY.md` 가 측정
한계로 미리 적어둔 그대로다: *"정확도는 안 잰다 — 모델이 가격을 틀리게 말해도 `success` 다."*
**잘못된 행동을 막았는가는 한 번도 잰 적이 없다.**

그리고 거기에는 의미 기억이 필요하다. 모델이 이미 가진 것은 ⑴ 눈앞의 스크린샷과
⑵ **안드로이드 앱 일반 관례에 대한 사전지식** 둘인데, 앱이 그 관례를 **어길 때** 사전지식은
적극적으로 틀리고 모델은 **자신 있게** 틀린다. 화면만 봐서는 어긋났는지 알 수 없다.

| 종류 | 예 |
|---|---|
| 관례 위반 | *"이 앱에서 뒤로 가기는 앱을 종료한다"* |
| 겉보기와 다름 | *"체크박스처럼 보이지만 라디오 버튼이다"* |
| 좌표 가정 무효 | *"이 키패드는 매번 숫자 배열이 바뀐다"* |
| 이름이 거짓말 | *"하단 탭의 '홈' 은 첫 화면이 아니라 광고 페이지다"* |

**화면에도 없고 사전지식에도 없다.** 기억밖에 담을 곳이 없고, **턴은 한 개도 안 줄일 수
있지만 실수를 막는다.** 그래서 원칙은 하나로 정리된다:

> **기억은 모델이 화면과 사전지식으로 알 수 없는 것만 담는다.**
> 그 "알 수 없는 것" 이 둘이다 — **다음 화면**(경로, 턴을 줄인다)과
> **이 앱의 예외**(관례 위반, 실수를 막는다).

⚠️ **양날이라는 것도 같이 봐야 한다.** 기억이 모델의 틀린 사전지식을 이기는 힘은, 틀렸을
때 **올바른 지각을 이기는 힘**과 같은 것이다(2차의 +27% — 없는 것을 네 번 스크롤했다).
이 방향으로 갈수록 검증층(Unit 6 점수 · 5c reconciliation · D층)이 더 중요해진다.

**Q13 — 정확도 지표는 아직 설계가 없다.** 필요한 것은 *"기억 없이 돌렸을 때 잘못된 행동을
몇 번 했나"* 같은 것인데, `run.outcome` 은 그걸 못 본다(20턴 안에 끝나면 `success` 다).
5b 프롬프트에는 이 갈래를 **먼저 넣어 뒀다** — 수치로 확인되기 전이라는 것을 알고 넣은 것이다.

⚠️ `PITFALL` 의 영토는 생각보다 좁다. §3 규칙 A 가 **안전 확인이 붙은 턴에서의 학습을
금지**하므로, "이 버튼은 위험하다" 류는 애초에 못 배운다. 거기에 순수 회피 정보가 효과
0 이면 남는 것이 얼마나 되는지 따져봐야 한다.

### ③ 그런데 경로는 템플릿에 **넣어도 산다** — 그래서 `RECIPE` 는 아직 필요 없다
C(10.2)는 경로를 `trigger → consequence` 한 칸에 욱여넣은 것인데도 −38.2% 를 냈다.
**효과의 대부분이 살아남았고 대가는 ~17%**다(그마저 사전 문턱 미달).

이것이 아래 '경로 지식은 APP_FACT 인가 RECIPE 인가' 절의 갈림길에 답을 준다:

| 그때 적어둔 것 | 이번 결과 |
|---|---|
| 경로를 **글로** 알려주니 턴이 줄었다 → `RECIPE` 의 추가 장치(`steps`·`precondition`·압축)가 **없어도 된다** | ✅ **이쪽** |
| 경로는 맞는데 글로는 안 줄었다 → 절감이 '판단 턴 건너뛰기'에서만 나온다 | ❌ |

**좋은 소식이다.** 아래쪽이었다면 좌표 매크로와 같은 방식으로 실패하는 설계로 가야 했다.

### ④ 그래서 5b 에 주는 지시
- **형식 비용은 문턱 미달이다.** 다만 방향은 일관됐다(C 9~11 vs B 8~10). "없다"가 아니라
  **"20% 문턱을 못 넘었다"** 로 읽을 것. n=4~5 다.
- **리플렉터가 뽑아야 할 것은 경로다.** 함정만 뽑으면 D 가 된다 — 효과 0, 나쁘면 역효과.
  프롬프트에 *"어디로 가면 닿는가"* 를 넣어야 한다.
- C 는 예산 2줄을 **꽉 채웠다.** 실전에서는 다른 기억과 경쟁하므로 이만큼 못 들어간다.

### ⚠️ 한계 — 같이 읽을 것
1. **과제 하나, n=4~5.** 일반화하지 말 것.
2. **`error` 2건을 사후 제외했다.** 사전 목록에 없던 규칙이다(1·2차에서 `error` 가 한 번도
   안 났다). 안 빼면 `turnsUsed=0` 이 평균을 A 16.5 → 13.2, B 8.8 → 7.0 으로 왜곡한다.
3. **D 의 `fail`(20턴)은 그대로 넣었다** — 과제의 결과이지 사고가 아니다. 다만 절단값이라
   D 의 진짜 평균은 18.0 **이상**이다.
4. **메가스터디에 보안 화면 판정이 걸렸다**(B 2회차). 무인 실행이라 즉시 실패했다 —
   이 앱으로 배치를 돌릴 때 재발할 수 있다.

---

## Unit 5 — 리플렉터 · 5a: §3 필터 (2026-09-16)

셋으로 쪼갰다. **5a 만 모델 없이 검증되는 조각**이라 먼저 했다.

| | 무엇 | 상태 |
|---|---|---|
| **5a** | §3 필터 — A층 템플릿 + 규칙 A(출처 차단) + B층 결정론 패턴 | ✅ 구현·패턴 검증 |
| 5b | 리플렉터 — 실행 끝 → 후보 추출 | ✅ **실기기 동작 확인** (2026-09-16, 아래) |
| 5c | reconciliation — `PENDING` 탈출구 | ⬜ **설계 확정, 구현 전**(아래) |

### ⚠️ A층은 **보장이 아니다** — 명세의 "넣을 자리 자체가 없다"는 과장이다 ★
`Candidate` 는 문장이 아니라 칸이다(`trigger` → `consequence`). 하지만 **칸을 채우는 건
모델**이고, 코드는 *"trigger 는 문자열"* 까지만 강제할 수 있지 *"trigger 에 동작이 들어
있다"* 는 강제하지 못한다. 모델은 얼마든지 `consequence` 에 `"잔액은 1,234,567원이다"` 를
넣을 수 있다.

A층이 실제로 하는 일은 **자리를 없애는 게 아니라 덜 주는 것**이다 — 자유 서술 칸이 없으니
모든 것을 동작→결과로 말하게 되고, 그만큼 화면 내용보다 앱 동작 쪽으로 기운다. 유용한
**편향**이지 보장이 아니다. 보장은 전부 결정론적 검사(길이·출처 대조·정규식)에서 온다.

⚠️ 그래도 **`Candidate` 에 자유 문장 칸을 더하지는 말 것.** `text` 를 그대로 받게 만들면
그 편향마저 사라지고, 남는 건 정규식(B)과 모델(C)뿐이다.

### 모델이 신고한 출처는 대조해야 한다 ★★
규칙 A 는 `sourceTurn` 으로 자르는데, **그 값을 주는 게 모델이다.** 7번 턴에서 뽑고도
`sourceTurn: 3` 이라고 적으면 — 악의가 아니라 그냥 틀려도 — **안전 규칙을 조용히
빠져나간다.** *"출처로 자르니 판정이 필요 없다"* 는 규칙 A 의 장점은 **출처가 사실일
때만** 성립한다.

그래서 `RunFacts`(그 실행의 실제 턴 번호·오염된 턴·실제로 간 패키지)를 `episode` 에서
만들어 대조한다:

| 검사 | 막는 것 |
|---|---|
| `sourceTurn` 이 실재하는 턴인가 | **규칙 A 우회** |
| `pkg` 가 그 실행에서 실제로 간 앱인가 | 간 적도 없는 앱에 기억이 붙는 것 |
| `trigger`/`consequence` 길이 상한(80/160) | 값을 욱여넣는 것 |

⚠️ **순서를 바꾸지 말 것** — `sourceTurn` 실재 확인이 오염 턴 검사보다 **먼저** 와야 한다.

조립(`assemble()`)은 **저장 시점에 한 번**, `Candidate` 안에서만 한다. 스키마에
`trigger`/`consequence` 칸이 없어서이기도 하고(설계 부록 DDL 도 `text` 하나뿐),
호출부가 제 나름대로 문자열을 이어 붙이기 시작하면 A층의 보장이 거기서 끝나기 때문이다.

### 규칙 A 는 **내용을 판정하지 않는다 — 출처로 자른다**
*"이게 안전 관련인가"* 를 문장으로 보려 하면 모델 판단이 끼어들어 비결정적이 된다.
어느 턴에 확인 카드가 떴는지는 **우리가 이미 안다**(`episode.hadSafety`, Unit 1 부터 기록 중).
그 턴에서 나온 후보를 통째로 버리면 판정이 필요 없다.

### ⚠️ 명세 B층의 '원문 대조'는 **구현하지 않았다** ★
*"후보의 토큰이 그 실행의 화면 텍스트에 그대로 있으면 의심한다"* 인데,
**우리는 화면 텍스트를 어디에도 저장하지 않는다.** `episode` 에 있는 건 모델이 쓴
`intent` 요약뿐이다.

**`intent` 로 대신하면 안 된다** — 후보가 `intent` 에서 파생되므로 토큰이 겹치는 게
**정상**이고, 그러면 전부 걸린다. 화면 텍스트를 저장하는 건 §3 이 막으려는 것을 DB 에
쌓는 일이라 **더 나쁘다.** 그래서 이 구멍은 열어 둔 채 기록한다 — 대신 리플렉터의 입력이
애초에 화면이 아니라 요약이라 **유출 경로가 그만큼 좁다.**

### B층이 못 잡는 것이 있다 — 그게 정상이다
| 후보 | B층 | 누가 잡나 |
|---|---|---|
| `"인증번호는 6자리이고 최근 값은 481920이었다"` | ❌ 버림 | B (숫자 4자리) |
| `"잔액은 1,234,567원이다"` | ❌ 버림 | B (금액) |
| **`"대화 목록 맨 위는 보통 '엄마'다"`** | ✅ **통과** | **C — 모델**(관계·고유명사) |
| **`"최근 검색어에 '이직'이 있었다"`** | ✅ **통과** | **C** |

숫자가 없는 개인정보는 정규식으로 못 잡는다. **B층만 세워 놓고 "§3 을 집행한다"고 쓰면
안 된다** — C층(reconciliation, 5c)과 D층(목록 UI)이 있어야 규칙 B 가 실제로 선다.

### ★ `PITFALL` 은 **앱 경계를 넘는다** — 그래서 기준이 더 엄해야 한다
| | 무엇으로 걸리나 | 어디까지 퍼지나 |
|---|---|---|
| `APP_FACT` | **패키지명** | 그 앱이 떠 있을 때만 |
| `PITFALL` | **목표 키워드** | **앱과 무관** — 목표에 그 말만 있으면 |

`APP_FACT` 에 개인 내용이 들어가면 그 앱 안에 머문다. 카톡 기억은 유튜브 작업에 안 붙는다
(`readForApp` 이 `pkg` 로 거른다). 그런데 **`PITFALL` 은 그 울타리가 없다** — 키워드가
`"보내"` 인 기억은 카톡이든 메일이든 문자든 붙는다.

즉 같은 내용이라도 **종류에 따라 노출 범위가 다르다.** C층 프롬프트는 `PITFALL` 후보를
더 엄하게 봐야 하고, 애초에 리플렉터가 `PITFALL` 을 뽑게 둘지도 다시 볼 문제다 —
① 순수 회피 정보는 **효과가 0 이었고**(위 템플릿 측정) ② 앱 경계를 넘고 ③ C층이 아직 없다.
`RECIPE` 를 읽는 쪽이 생길 때까지 드롭다운에서 뺀 것과 같은 논리다.

### 왜 개인 내용을 막나 — 논거를 정확히 하면
⚠️ 흔히 드는 두 가지는 **구분점이 아니다**:

- **"목적 외 전송"** — 모든 기억에 일어난다. 설정 앱을 열면 *"디바이스 케어 → 저장공간"* 도
  작업과 무관하게 붙는다. 차이는 *일어나느냐* 가 아니라 **대가**다: 구조는 토큰 낭비고,
  개인 내용은 **그 사람의 데이터가 안 물어본 일로 전송되는 것**이다.
- **"노출이 폭발한다"** — `APP_FACT` 는 앱을 못 넘으므로 폭발까지는 아니다. 정확히는
  **화면 방문 단위 → 앱 체류 단위**로 오른다(대화 목록을 볼 때만 → 카톡에 있는 동안 내내).

**남는 진짜 이유는 둘이다:**
1. **못 지운다.** 기억을 지워도 `episode.note` 에 문장이 남고(실측), 이미 API 로 나간 것은
   되돌릴 방법이 없다.
2. **금방 틀려진다.** 앱 구조는 몇 달 가지만 *"대화 목록 맨 위"* 는 몇 시간이다. 그리고
   **틀린 기억은 +27%** 였다(2차 측정). 사생활을 빼고 **효용만 따져도** 손해다.

### 패턴 검증 (명세 §3 판정표 + 오탐 후보, 17/17)
`3단계` · `6자리다` · `5 bottom tabs` · `Android 14` 는 통과하고,
`481920` · `1,234,567원` · `010-1234-5678` · `user@example.com` · `42.0 GB` 는 버려진다.
자릿수를 **말하는 것**(`6자리다`)은 값이 아니라 형식이라 통과하는 게 맞다.

**완벽할 필요가 없다 — 비용이 비대칭이다.** 오탐(구조인데 버림)은 기억 하나를 잃을 뿐이고,
미탐(내용인데 통과)은 **개인정보를 영구 저장**한다. 게다가 기억은 **재관측이 가능**해서
오늘 버려도 같은 앱을 다음에 쓰면 다시 뽑힌다. 버리는 비용이 거의 0이므로 **애매하면 버린다.**
`Android 14.0` 같은 게 걸리는 것도 그래서 받아들인다.

### 리플렉터가 만드는 기억은 `PENDING` · `score 1` 이다
`Candidate.toMemory()` 가 그렇게 박아 둔다. 사람이 쓴 것의 `HUMAN_SCORE(2)` 를 쓰면
안 된다 — **아직 아무도 검증하지 않았다.**

---

## Unit 5b — 리플렉터 (2026-09-16) ★

실행이 끝나면 그 실행의 **기록을 읽고** 후보를 뽑는다. 저장은 항상 `PENDING` · `score 1`.

### 실기기 첫 동작 — 기대보다 좋다
기억 0건에서 mega 과제(15턴 성공)를 돌리자 **후보 1건**이 나왔다:

```
APP_FACT · PENDING · score 1 · source=reflector · pkg=net.megastudy.smartplay.main
"Clicking the menu icon and selecting '메가선생님' → Accesses the teachers list
 categorized by subject to view teacher profiles and curriculums."
```

**사람이 손으로 써서 −47% 를 냈던 그 경로 문장과 사실상 같은 내용**이다. 그리고
*부정 사실이 아니라 경로*를 골랐다 — 프롬프트에 넣은 *"후보를 가장 많이 지우는 것"* 규칙이
의도대로 작동했다는 첫 신호다. 15턴짜리 로그에서 **1건만** 낸 것도 좋다(0건이 정답인
경우가 많다는 규칙이 과잉 추출을 막았다). `sourceRunId`·`pkgVersion(5074)` 도 제대로 박혔다.

### ★ 두 번째 실행이 5c 의 근거를 실측으로 줬다
같은 과제를 한 번 더 돌리자 **같은 사실을 다른 문장으로** 또 뽑았다:

```
#65  Clicking the menu icon and selecting '메가선생님' → Accesses the teachers list …
#66  opening the hamburger menu, selecting '메가선생님', and choosing a subject → displays …
```

**문자열 비교로는 영원히 안 묶인다.** 명세가 reconciliation 을 *"쓰기의 필수 부품"* 이라고
부른 이유가 이것인데, 이제 **추측이 아니라 관측**이다. 둘 다 `PENDING`·`score 1` 로 남아
**어느 쪽도 영영 승격되지 않는다** — 검역실에 입구만 있고 출구가 없는 상태다.

### 설계에서 정한 것
| | |
|---|---|
| **호출 시점** | `onRunEnd` 가 **큐에 넣고 즉시 반환**. 거기서 그냥 돌리면 소켓 응답이 늦어져 측정 벽시계가 오염되고, `agentBusy` 가 잡혀 있어 다음 `RUN` 이 거절된다(하네스는 응답을 받자마자 다음 `RUN` 을 보낸다 — **가끔** 깨지는 제일 찾기 어려운 종류다) |
| **겹침 방지** | **단일 스레드 큐**. 별도 깃발을 안 쓰는 이유 — 깃발은 샐 수 있고(`agentBusy` 가 true 로 새면 영구히 막힌다) 큐에는 그 실패 모드가 없다. 밀리면 쌓일 뿐이다 |
| **`agentBusy`** | 종전대로 에이전트 루프가 끝나면 뗀다. 그 팻말이 지키는 것은 **화면**인데 리플렉터는 화면을 안 만진다 |
| **스위치** | `기억 관리` 화면의 체크박스. **기본 꺼짐** |
| **사람이 보는 면** | 실행 로그가 아니라 **기억 목록**. 되새김은 실행이 끝난 뒤라 그 실행의 로그 스트림이 이미 닫혔다 |

### ⚠️ 새 엔드포인트 — `cuCall` 의 규칙을 가져오지 말 것 ★
리플렉터는 `:generateContent` 를 쓴다(글 주고 JSON 받기). 이 저장소에 *"이 서버는 중첩을
안 받는다"* 가 세 번 적혀 있지만 **그건 전부 `/interactions` 이야기**다. 일반
`generateContent` 는 문서·SDK 형식이 맞고 `thinking_config` 도 **중첩**이 정상이다.
**반대 규칙을 쓰는 두 주소**이므로 복사하면 400 이다.

지금은 사고수준을 **아예 안 보낸다** — 안 보내면 그 함정을 밟을 일이 없고, 리플렉터에
사고수준이 필요하다는 근거도 없다. `responseMimeType=application/json` 으로 JSON 을 강제하되
코드펜스를 씌워 오는 경우가 있어 파싱에서 한 번 더 벗긴다.

### ⚠️ C층이 없다는 게 지금 그대로 보인다
저장된 후보에 `'메가선생님'` 이 들어 있다. **UI 라벨이라 저장이 맞다.** 그런데 B층(정규식)은
`'메가선생님'`(라벨)과 `'엄마'`(사람)를 **구분할 방법이 없다** — 숫자도 이메일도 아니다.
지금 그걸 가려낼 유일한 장치는 **D층(사람이 목록에서 봄)** 이고, 5c 에서 C층이 붙기 전까지
**자동 추출을 켠 채 방치하면 안 된다.** 기본값이 꺼짐인 이유이기도 하다.

### ★★ 5c 의 함정 — C층의 질문은 "고유명사인가"가 아니다
명세의 집행표에 C층이 *"이름 · 관계 · **고유명사** · 행태 추론"* 이라고 적혀 있다.
**이걸 그대로 프롬프트에 옮기면 우리가 가진 최고의 기억이 걸러진다** — `'메가선생님'` 도
`'디바이스 케어'` 도 `'세탁하기'` 도 전부 고유명사다. −47% 를 낸 그 문장이 바로 그것들로
이루어져 있다.

**질문은 "고유명사인가"가 아니라 "이 사람의 것인가" 여야 한다.**

| 후보 | 기대 | 왜 |
|---|---|---|
| `'메가선생님'` 메뉴 → 강사 목록 | ✅ 통과 | **앱**이 모든 사용자에게 똑같이 그리는 라벨 |
| `'디바이스 케어'` → 저장공간 | ✅ 통과 | 〃 |
| `'세탁하기'` → 기계 선택 팝업 | ✅ 통과 | 〃 |
| 대화 목록 맨 위 → `'엄마'` 방 | ❌ 차단 | **사용자의** 연락처 데이터 |
| 검색창 → 최근 검색어 `'이직'` | ❌ 차단 | 〃 |

**앱이 그린 글자는 통과, 사용자가 만들었거나 사용자에게만 뜨는 글자는 차단.**
그리고 `'메가선생님'` 이 없으면 그 기억은 **쓸모가 없다** — *"메뉴를 누르면 뭔가 나온다"* 로
모델이 할 수 있는 게 없다. 어느 항목을 누르는지가 정보의 전부다.

⚠️ **이 구분은 코드로 못 한다.** 둘 다 화면에 뜬 짧은 한국어 명사다. 정규식도, 접근성
노드도(둘 다 그냥 `text` 속성) 못 가른다. **재관측 횟수로도 안 된다** — 그 사람의 연락처도
매번 똑같이 재관측된다. 뜻을 알아야 갈리고, 그래서 C층이 모델이다.

---

## Unit 5c — reconciliation 설계 (2026-09-16 확정, 구현 전) ★

### 왜 — 실측으로 확인된 것
리플렉터가 **같은 사실을 매번 다른 문장으로** 쓴다(`#65`/`#66`, Unit 5b 절).
문자열 비교로는 영원히 안 묶이고 둘 다 `PENDING`·`score 1` 로 남아 **어느 쪽도 영영
승격되지 않는다.** 명세 표현으로 *"정리 기능이 아니라 승격 카운터의 전제"* 다.

### ① 리플렉터와 **합친다** (호출 1회)
`ADD` 는 리플렉터가 후보를 낸 순간 이미 정해진 것이고, 무엇보다 **로그를 읽은 쪽이
판정해야 근거가 있다** — `#65`/`#66` 이 둘 다 턴 9~11 을 가리킨다는 것은 로그를 본 쪽만
안다. `DELETE`·`UPDATE` 도 *"이번 실행에서 어긋났다"* 는 증거가 로그에 있다.

⚠️ **대가**: 한 프롬프트가 세 일(추출 · 대조 · 내용 판정)을 한다. 실패해도 **어디서
실패했는지 분리가 안 된다** — 1차 측정에서 세 과제가 각각 다른 이유로 오염돼 아무 결론도
못 실었던 것과 같은 함정이다. **완화책으로 판정마다 `why` 를 받아 로그에 남긴다.**

```json
{"verdict":"NOOP",   "id":65, "why":"턴 9~10 에서 같은 경로를 다시 봤다"}
{"verdict":"ADD",    "trigger":"…", "consequence":"…", "pkg":"…", "source_turn":9}
{"verdict":"UPDATE", "id":40, "trigger":"…", "consequence":"…", "why":"…"}
{"verdict":"DELETE", "id":12, "why":"턴 5 에서 반대로 동작했다"}
```

### ② 입력 — `PENDING` 은 넣고 `RETIRED` 는 뺀다
| | |
|---|---|
| `ACTIVE` | ✅ |
| **`PENDING`** | ✅ **반드시** — 재관측이 **유일한 승격 경로**다. 단 *"또 내지 마라"* 가 아니라 **"같은 걸 봤으면 그렇다고 말해라"** 로 물어야 한다 |
| `RETIRED` | ❌ 부활 경로를 만들면 불안정하다 |

범위는 **이 실행에 나온 패키지들의 기억 전부**다(5b 는 *주입된 것*만 본다).

### ③ 판정 처리
| 판정 | 처리 | `pinned` |
|---|---|---|
| `ADD` | 새 행 · `PENDING` · `score 1` | — |
| `NOOP` | **`score +1`** → `≥2` 면 `ACTIVE` (`activateProven` 이 드디어 작동) | — |
| `DELETE` | **즉시 `RETIRED` + `invalidAt`** | ✅ 면제 |
| `UPDATE` | 기존 `RETIRED` + `supersededBy` 에 새 id | ✅ 면제 |

### ④ `RETIRED` 로 가는 길이 둘 — 성격이 다르다 ★
```
점수 경로   누적 · 간접 · 결정론   "이 기억이 붙은 실행이 자꾸 실패한다"   (Unit 6)
판정 경로   일회 · 직접 · 모델     "이번 실행이 이 기억과 어긋났다"       (5c)
```
**판정 경로만이 "성공했는데 틀린 기억"을 잡는다.** 메타클럽 케이스가 그렇다 — 기억이
거짓인데 에이전트가 우회해서 **성공**하면 `run.outcome = success` 라 Unit 6 카운터는
**오히려 `+1` 을 준다.** 실패 기반 강등은 이걸 **구조적으로 못 잡는다**(2차 meta 조건의 B 는
7,6,10,9,6 으로 대부분 성공했다).

### ⑤ `pinned` — 원칙을 하나로
> **코드가 재는 것은 사람 것에도 적용한다. 모델이 판정하는 것은 사람 것에 적용하지 않는다.**

| 경로 | 누가 | `pinned` |
|---|---|---|
| 실패 기반 강등 (`lowerScore`) | 코드 (`run.outcome`) | ❌ **적용** — 사람도 틀린다(메타클럽 문장은 사람이 썼다) |
| 감쇠 · 용량 상한 (Unit 8) | 코드 (시간) | ✅ 면제 — `pinned` 의 **원래 의도** |
| `DELETE` · `UPDATE` | **모델** | ✅ 면제 |

⚠️ **지금 `lowerScore` 에 `AND pinned = 0` 이 있어서 UI 로 쓴 기억은 강등이 아예 안 걸린다.**
목록 UI 가 새 기억에 `pinned` 를 기본 체크하므로, 어제 정한 `HUMAN_SCORE = 2` 가
**`bench` 기억에만 유효한 상태**다. 이 조건을 빼야 제 일을 한다.

### ⑥ 회복 — 잘못 죽여도 자가 치유된다
`RETIRED` 는 **자동 복귀가 없다**(`activateProven` 이 `PENDING` 만 본다). 대신 입력에서
제외되므로, 같은 사실이 재관측되면 모델은 그런 기억이 있는지도 모른 채 `ADD` 를 낸다 →
**새 행으로 처음부터**(`PENDING`·`score 1`). 지식이 사라지는 게 아니라 **증거를 다시 쌓는다.**
잃는 것은 **누적된 점수**뿐이다. §3 의 *"버리는 비용이 거의 0"* 과 같은 성질이다.

### ⑦ C층(내용 판정)이 같은 프롬프트에 붙는다
기준은 *"고유명사인가"* 가 **아니라** *"이 사람의 것인가"* 다(위 '5c 의 함정' 절의 판정표).
`PITFALL` 은 **앱 경계를 넘으므로** 더 엄하게 본다.

### 검토했다가 버린 것 — 되살리기 전에 읽을 것
| 대안 | 왜 버렸나 |
|---|---|
| `DELETE` → `score −1` | **성공 보너스와 상쇄된다**(`+1` `−1` = 0). `DELETE` 가 잡으려던 바로 그 경우(성공했는데 틀린 기억)에 무력해진다 |
| `DELETE` → 사다리(`ACTIVE`→`PENDING`→`RETIRED`) | `PENDING` 은 주입이 안 되므로 **점수 경로가 닫혀** 틀린 기억이 검역실에 눌러앉는다. `PENDING` 이 *"새 후보"* 와 *"반박당한 것"* 두 종류로 섞여 D층(사람)이 구분을 못 한다 |
| `PENDING` 을 `ALREADY KNOWN`(또 내지 마라)에 넣기 | **재관측 = 승격 신호**인데 그걸 막는 셈이다. `ACTIVE` 는 넣어도 되지만(점수가 *사용*으로 오른다) `PENDING` 은 안 된다 |

### 구현 목록
1. 프롬프트를 후보 추출 → **판정 출력**으로
2. 입력 범위 확대 (`injectedIn` → 이 패키지들의 `ACTIVE`+`PENDING` 전부)
3. 판정 처리 코드 (`NOOP`/`ADD`/`UPDATE`/`DELETE`)
4. `lowerScore` 에서 `AND pinned = 0` 제거
5. C층 규칙을 프롬프트에 + 판정표로 시험

🧪 **시험 재료가 이미 DB 에 있다** — `#65`·`#66` 중복 쌍. 켜자마자 `NOOP` → `score 2` →
`ACTIVE` 승격이 도는지 바로 볼 수 있다.

---

## Unit 6 — 승격 카운터 (2026-09-16)

**두 덩어리 중 앞엣것만 구현했다.** 승격 카운터는 `run.outcome` 만으로 돌아 지금 검증
가능하고, reconciliation 은 **후보를 만드는 리플렉터(Unit 5)가 있어야 입력이 생긴다.**
표에 6 을 🔶 로 둔 이유다 — "6 을 끝냈다"의 의미가 두 갈래라 뭉뚱그리면 안 된다.

### 새로 만든 것
`memory_recall` 표 — **어느 실행에서 어느 기억이 주입됐나.** 행 하나 = 기억 하나 × 실행 하나.

| 칸 | |
|---|---|
| `runId` · `memoryId` | 복합 기본키 — **"한 실행은 최대 +1"이 코틀린 규칙이 아니라 스키마 제약이 된다** |
| `injections` | 그 실행에서 몇 번 주입됐나 |
| `createdAt` | |

**왜 RAM 이 아니라 표인가** — 점수만 남기면 `score = 3` 을 보고 "성공 3번인가, 성공 5번에
실패 2번인가"를 구분할 수 없다. Unit 4 에서 *"기억이 정말 주입됐나"* 를 `episode.note` 로
확인해야 했던 것과 **같은 질문이 점수에도 온다.**

⚠️ **다만 지금 이 표를 읽는 곳은 목록 UI 한 군데뿐이다**(`recallStats` → `N개 실행(성공 M)`).
**점수 계산조차 이 표를 안 읽는다** — `settleRun` 은 RAM 스냅샷으로 곧장 UPDATE 를 친다.
즉 **오늘은 이 표가 없어도 카운터는 똑같이 동작한다.** 복합 기본키도 같은 이유로 지금은
이중 안전장치다(`settleRun` 이 이미 중복을 없애고 넘긴다) — 다른 경로가 이 표에 쓰기
시작할 때 비로소 실제 방어선이 된다.

**✅ 2026-09-16 갱신 — Unit 5 에서 곧바로 쓰인다.** 설계 §2 가 리플렉터의 입력을 셋으로
잡는데 그 세 번째가 **"그 실행에 주입됐던 기억"** 이다. 없으면 *"리플렉터가 이미 아는 것을
매번 새로 발견한다"*. 명세 부록의 `injection` 표가 이것과 같은 것이다(같은 복합 기본키·
같은 CASCADE). ⚠️ 다만 명세 DDL 은 `memory_id ... REFERENCES memory(id)` 라, Room 이 FK 를
켜는 이 앱에서 그대로 따랐으면 **`MEMCLEAR` 가 제약 위반으로 실패**했을 것이다 — `memory`
로 가는 FK 를 뺀 판단이 결과적으로 맞았다(실기기 확인).

**그리고 애초에 지금 만든 이유는 소급이 안 되기 때문이다.** 쓸 곳은 전부 앞에 있고
(Unit 7 랭킹의 `COUNT(*)` vs `SUM(injections)`, Unit 8 의 감쇠, 측정 자동화),
나중에 만들면 **마이그레이션을 두 번 하고도 그 사이 실행 이력은 영영 비어 있다.**
"지금은 쓰기 전용에 가깝다"는 사실이므로, 이 비용이 아깝다고 판단되면 걷어내는 것도
정당하다 — `settleRun` 의 `insertRecalls` 한 줄과 표 하나다.

### 점수 규칙 — `outcome` 넷 중 **둘만 증거다** ★
| `outcome` | 언제 | 점수 |
|---|---|---|
| `success` | 모델이 완료 선언 | **+1** (상한 4) |
| `fail` | 최대 턴 도달 | **−1** (`pinned` 면제) |
| `aborted` | 사용자 중단 · 안전 확인 거부 | **건드리지 않음** |
| `error` | 예외 — 화면 꺼짐 · 네트워크 · dispatch 실패 | **건드리지 않음** |

⚠️ **`aborted`·`error` 를 실패로 묶으면 Wi-Fi 가 끊긴 것만으로 멀쩡한 기억이 두 번 만에
RETIRED 로 내려간다.** 게다가 `runAgent` 의 `outcome` **초기값이 `"error"`** 라
("예외로 빠져나가면 아무도 못 덮는다") *"성공이 아니면 깎는다"* 로 짜는 순간 **모든 예외가
곧 강등**이 된다. 반드시 두 값을 이름으로 집어 분기할 것.

주입 기록 자체는 네 경우 모두 남긴다 — 점수와 무관하게 **주입은 일어난 사실**이다.

전이는 `score` 를 따라간다: `PENDING` 이 `score ≥ 2` → `ACTIVE`(검역 해제),
`score ≤ 0` → `RETIRED` + `invalidAt`(삭제가 아니라 기록 — 사람이 되살릴 수 있다).

### ⚠️ `PENDING → ACTIVE` 는 이 카운터로는 **절대 안 일어난다** ★
구현하고 나서야 알았다. **PENDING 은 주입되지 않는다**(읽기 쿼리가 `state='ACTIVE'` 만
본다) → 버퍼에 안 들어간다 → `settleRun` 이 못 본다 → **`score` 가 영원히 안 오른다.**

명세를 다시 읽으면 승격 경로가 원래 다른 데 있다:

| 규칙 | 무엇이 올리나 | 언제 온다 |
|---|---|---|
| **재관측 2회 → ACTIVE** | reconciliation 의 `NOOP` (리플렉터가 같은 사실을 또 뽑음) | **Unit 5 뒤** |
| 주입된 실행이 성공 → `score+1` (§7 개정 3) | 이번에 만든 카운터 | 지금 |

**둘은 다른 규칙이다.** 앞은 *검역 해제*이고 뒤는 *이미 ACTIVE 인 것의 랭킹용 신뢰도*다.
`activateProven` 을 `settleRun` 에 걸어 둔 것은 해롭지 않지만(조건이 안 맞아 0행 갱신)
**지금은 죽은 코드**이고, reconciliation 이 올 때 그쪽에서 불러야 한다.

### 사람이 넣은 기억은 `score 2` 로 시작한다 (열린 결정 N5, 닫힘) ★
기본값 1 이면 `fail` **한 번에 `score 0` → `RETIRED`** 다. 그리고 RETIRED 는 주입에서 빠진다.

**측정에 직접 위험하다** — 30회 배치 중 B 실행 하나가 20턴을 넘기면 그 순간 기억이
은퇴하고 **이후 모든 B 가 사실상 A 가 된다.** 실패가 아니라 *조건이 바뀌는* 것이라
집계에서 걸러지지도 않는다. `memoryReadFailed` 와 같은 종류의 조용한 오염이다.

게다가 `fail` 은 *"기억이 틀렸다"* 가 아니라 *"20턴 안에 못 끝냈다"* 이다.

그래서 `MemoryGateway.HUMAN_SCORE = 2` 를 두고 **사람이 넣는 경로**(목록 UI 의 새 기억,
소켓 `MEMADD`)만 거기서 시작한다. 한 번 실패해도 ACTIVE 를 유지하고 두 번이면 은퇴한다 —
승격 쪽의 *"한 번은 우연일 수 있다"* 와 대칭이다.

⚠️ **리플렉터가 만든 기억에는 쓰지 말 것.** 그쪽은 검증 안 된 후보라 `score 1` +
`PENDING` 에서 시작해야 한다. 편집으로 기존 기억을 저장할 때도 안 건드린다(`copy()`).

### RAM 버퍼를 거치는 이유 — 주입 시점에는 `runId` 가 없다 ★
`readForApp`/`readForTask` 는 `Executor` 계약을 통해 불리는데 그 계약에 `runId` 가 없고,
넣으면 **판단 코어가 기억을 알게 된다.** 그래서 `MemoryGateway` 가 id 를 모아 뒀다가
`runId` 가 확정된 `onRunEnd` 에서 표로 내보낸다.

⚠️ **버퍼를 실행 *시작*에서 비우면 안 된다.** `runAgent` 는 `taskNote()` 를 `onRunStart`
보다 **먼저** 부른다 — 시작에서 비우면 방금 담긴 PITFALL 이 지워져 **그 기억은 영영 점수를
못 받는다.** 읽기 실패 깃발이 "종료에서 소비"인 것과 정확히 같은 함정이다. 지금은
`settleRun` 이 **스냅샷을 뜨고 곧바로 비운 뒤** DB 를 만진다 — DB 가 실패해도 이번 실행의
id 가 다음 실행으로 새지 않는다.

⚠️ `RoomRunTrace.onRunEnd` 의 **`swallow` 가 둘로 나뉘어 있다.** 한 블록에 묶으면
`finishRun` 이 실패했을 때 `settleRun` 이 아예 안 불리고, 그러면 버퍼가 안 비워져
**이번 실행의 기억이 다음 실행의 결과로 채점된다.**

### `numRecalled` 는 '턴 수'가 아니다 — 랭킹이 이미 이걸로 정렬한다 ★
`runAgent` 는 `note()` 를 턴이 아니라 **`function_call` 마다** 부른다. 그래서
`numRecalled` 도 `injections` 도 **주입 횟수**다(대개 1턴 = 1호출이라 값이 같지만
**같다고 가정하면 안 된다**).

문제는 `activeAppFacts` 가 `ORDER BY numRecalled DESC` 라는 것이다:

| | `numRecalled` |
|---|---|
| 한 실행에서 20번 붙음 — 모델이 그 앱에서 헤매다 **실패** | **20** |
| 20개의 서로 다른 실행에 한 번씩 — 전부 **성공** | **20** |

**근거의 강도가 전혀 다른데 같은 숫자다.** 예산이 2줄이라 이 순서가 곧 "무엇이 잘리느냐"다.
`memory_recall` 이 `COUNT(*)`(실행 수)와 `SUM(injections)`(주입 횟수)를 **나눠서** 주므로
Unit 7 이 어느 쪽을 쓸지 **지금 정하지 않아도 된다.** 그때까지 정렬은 종전대로 둔다.

### 삭제 정책 — `memory` 로 가는 외래키는 **일부러 없다**
사람이 기억을 지워도 `memory_recall` 행은 남는다.

⚠️ **근거를 "기록 대 기억"으로 세우면 안 된다.** `episode` 는 이 설계에서 **일화 기억**이고
(엔티티 주석: *"액션 1건 = 일화(EPISODE)"*) Unit 5 리플렉터의 원재료다 — 기록이면서 기억이다.
선은 다른 데 있다: **모델에 주입되는 표는 `memory` 하나뿐이다.**

그래서 기억을 지우는 건 *"앞으로 주입하지 마라"* 이지 *"그런 일이 없었다"* 가 아니다.
주입 기록까지 지우면 **과거 실행의 해석이 소급해서 망가진다** — "이 실행은 조건 B 였나"를
나중에 물을 수 없게 된다(Unit 4 에서 실제로 물었던 질문이다). `memory.id` 가
AUTOINCREMENT 라 재사용되지 않으므로 지워진 기억의 행이 다른 기억으로 오인될 위험도 없다.

반대로 `run` 에는 CASCADE 를 건다 — 실행 로그를 정리하면(Unit 8) 그 실행의 주입 기록도
같이 사라지는 게 맞다.

### 검증 상태
| | |
|---|---|
**실기기 검증 완료 (2026-09-16, SM-S931N)** — 실행 7회.

| | |
|---|---|
| 마이그레이션 DDL == `schemas/4.json` | ✅ 문자 단위 대조 |
| 스키마 3→4 실기기 | ✅ `user_version=4`, **기존 데이터 생존**(run 95 · episode 707 · memory 1), `a11mem` 경고 없음 |
| 성공 → `+1`, **실행당 1회** | ✅ `2→3`. 같은 실행에서 `injections=2` 인데 점수는 **+1** — 이게 핵심 검증이다 |
| 상한 4 | ✅ 4에서 또 성공해도 4 (`numRecalled` 만 오름) |
| 실패 → `−1` | ✅ `2→1`, ACTIVE 유지 |
| 은퇴 | ✅ `1→0` → `RETIRED` + `invalidAt` 기록 |
| RETIRED 는 주입에서 빠진다 | ✅ 이후 실행에서 `numRecalled` 안 오름 · recall 행 안 생김 |
| `pinned` 면제 | ✅ **실패 2회에도 `score 2` 그대로** |
| `aborted` 는 점수를 안 건드린다 | ✅ 주입은 되고(recall 행 `outcome=aborted`) 점수는 그대로 |
| 버퍼가 실행 간에 안 샌다 | ✅ `injections` 이 누적되지 않음 (아래) |
| 기록은 결과와 무관하게 남는다 | ✅ `fail`·`aborted` 실행에도 행 생성 |
| 목록 UI 표시 | ✅ `점수 2 · 고정` / `주입 3회 · 3개 실행(성공 0)` / `RETIRED · 점수 0 · 무효화됨` |
| `error` 경로 | ⬜ **미검증** — `aborted` 와 **같은 `else` 분기**라 코드상 동일하다 |
| ~~`PENDING → ACTIVE` 전이~~ | ⛔ **잴 수 없다** — 위 참조. reconciliation 과 함께 온다 |

**버퍼 격리는 우연히 더 깨끗하게 확인됐다.** 저장공간 실행(주입 2회) **직후** 시계 실행을
돌렸는데, 시계 실행의 recall 행이 `injections=1` 이었다. 버퍼가 안 비워졌다면 `3` 이었을
것이다. 참고로 시계 실행에서 주입이 일어난 건 버그가 아니다 — **앞 실행이 설정 앱을 띄워
둔 채 끝나서** 첫 턴의 포그라운드가 설정이었다.

**`MEMCLEAR` 로 지운 기억의 recall 행이 남는 것도 실측으로 확인**했다(위 삭제 정책).

⚠️ **실기기 검증은 `source=bench` 기억으로 할 것.** 목록 UI 로 넣으면 `pinned` 가 기본
체크(자동 강등 면제)라 **강등 경로가 아예 안 돈다.** 소켓 `MEMADD` 로 넣은 것은
`pinned=false` 다.

⚠️ 스키마를 올렸으니 첫 실행 뒤 **반드시** `adb logcat -s a11mem:W` 를 볼 것 —
마이그레이션이 어긋나도 **앱은 멀쩡히 돌고 DB 만 조용히 죽는다.** (역으로, 이번이 그
'읽기 실패 경로'를 처음 밟아 볼 기회이기도 하다 — Unit 3 에서 미검증으로 남은 항목.)

---

## Unit 4 2차 결과 — **기억은 맞으면 −49%, 틀리면 +27%** (2026-09-16) ★

1차의 세 결함(바닥 효과·주입 버그·행동 불가능한 문장)을 전부 고치고 다시 쟀다.
30회 전부 성공, 제외 0건.

| 과제 | A 평균 | B 평균 | 변화 | A / B 낱값 |
|---|---|---|---|---|
| storage | 7.2 | 7.8 | +8.3% | 7,7,7,8,7 / 7,7,9,8,8 |
| meta | 6.0 | 7.6 | **+26.7%** | 6,6,6,6,6 / 7,6,10,9,6 |
| **mega** | **17.2** | **8.8** | **−48.8%** | 18,18,18,17,15 / 8,8,9,8,11 |

**사전 기준(20% × 3중 2과제)으로는 1/3 이라 여전히 "효과 없음"이다. 그 판정은 유지한다.**
하지만 1차와 달리 **무슨 일이 일어났는지가 분명하다.**

### mega — 기억이 맞으면 크게 듣는다
A `18,18,18,17,15` / B `8,8,9,8,11`. **겹치는 구간이 하나도 없다.** 검색으로 헤매던
9~10턴이 통째로 사라졌다. 조건이 맞을 때의 효과 크기가 이 정도다.

### meta — **기억이 틀리면 해롭다** ★★
내가 쓴 문장: *"'최근 이용내역' is on the home screen."*
**거짓이었다.** 홈 화면엔 하단 탭 다섯 개(세탁하기·전환하기·참여적립·팀플·티켓예매)뿐이고
`최근 이용내역` 은 `세탁하기` 를 거쳐야 나온다(캡처로 확인).

기억 없는 A 는 6,6,6,6,6 으로 **완벽하게 안정적이었다** — 낭비가 애초에 없었다.
내가 턴 4·5(`세탁하기` → 팝업 닫기)를 "우회"로 **오독**했다. 그게 경로 자체였다.

틀린 기억을 받은 B 가 한 일:
```
턴 4  drag  "홈 화면에 최근 이용내역이 있는지 아래로"
턴 5  drag  "더 아래로"
턴 6  drag  "더 아래로"
턴 7  drag  "더 아래로"          ← 없는 것을 네 번 찾았다
턴 8  click 프로필/포인트 영역
```

### ⚠️ 이것이 뒤집는 것 — "기억은 틀려도 안전하다"는 주장
나는 이 설계를 이렇게 옹호했다: *"좌표는 낡아도 모델이 알 수 없어 조용히 틀리지만,
이름은 화면에 없으면 모델이 보고 무시한다."*

**데이터가 아니라고 말한다.** 모델은 무시하지 않았다. **기억이 약속한 것을 찾아 네 번
스크롤했다.** 기억은 **자기 눈보다 기억을 믿게 만든다.**

Unit 5 에 직결된다 — 리플렉터가 **가끔만 틀려도** 에이전트를 적극적으로 망가뜨린다.
명세서 §3 필터는 *사생활·안전*을 거르지 **사실 여부를 못 본다.** 그래서
`state=PENDING` → reconciliation(Unit 6) 이 **리플렉터보다 먼저** 서 있어야 한다.

### storage — 낭비가 이미 없었다
A 7턴이 `설정 → 검색 → "저장공간" → 결과 → Storage → wait` 로 **이미 최단**이다.
1차 때 보이던 스크롤 방황이 지금은 안 나온다(모델이 검색을 안정적으로 쓴다).
기억이 맞는 말이었지만 **줄일 것이 없었다.** B 의 추가 턴은 읽기형 스크롤이다.

### 그래서 이번에 실제로 배운 것
> **기억의 효과는 "있다/없다"가 아니라 조건부다.**
> · 사실이고 · 진짜 탐색형 낭비를 겨냥하고 · 행동으로 옮겨지면 → **−49%**
> · 하나라도 어긋나면 → **0 또는 해로움(+27%)**

**질문이 옮겨간다.** "기억이 도움이 되나"는 답이 나왔다(조건이 맞으면 크게 된다).
남은 질문은 **"그 조건을 만족하는 기억을 신뢰성 있게 만들어낼 수 있나"** 이고,
그게 정확히 리플렉터(Unit 5)의 문제다. 이제 **실패의 대가도 안다.**

### 방법론 교훈 — 문장은 화면을 보고 써야 한다
musinsa 때는 상품 페이지를 캡처해 확인했는데 **메타클럽은 안 했다.** 실행 로그만 읽고
"턴 4·5 는 우회"라고 해석했다. 3/3 재현이라 확신했는데, **재현되는 오독**이었다.
**규칙: 기억 문장의 근거는 로그가 아니라 화면이다.**

---

## Unit 4 결과 — 판단 ① : **효과 없음** (2026-09-16) ★

30회 전부 성공, 제외 0건. 사전에 정한 기준(평균 턴 20% 이상 감소 × 3과제 중 2과제)에
**한 과제도 못 미쳤다.**

| 과제 | A 평균 | B 평균 | 변화 | 완주 |
|---|---|---|---|---|
| melon | 4.2 | 4.0 | −4.8% | 5/5 · 5/5 |
| storage | 7.4 | 7.2 | −2.7% | 5/5 · 5/5 |
| musinsa | 9.6 | 9.6 | **±0.0%** | 5/5 · 5/5 |

원자료: `docs/bench/memory_ab.jsonl`

### 측정을 무효로 만들 수 있었던 것들은 확인했다
| | |
|---|---|
| 기억이 **실제로 주입**됐나 | ✅ B 실행 `episode.note` 에 문장 그대로, **A 는 0턴** |
| 모델·사고수준 | ✅ 30행 전부 `3.5-flash/low` |
| 조건 A 가 정말 0건 | ✅ 실행마다 `memoryCount` 확인 |
| 검정력 | ✅ 턴 수 분산이 아주 작다(7,8,7,7,7 꼴). 20% = 1.5턴이면 **명백히 보였을 것** |

### 그런데 세 과제 중 **깨끗한 건 하나뿐**이다 — 나머지는 내 설계 실수다

**① melon 은 잴 여유가 없었다(바닥 효과).** 본 측정 A 가 4.2턴인데 그 구성이
`list_apps + open_app + click + wait` 다. **모델이 이미 곧장 곡을 누른다.** 기억은 이미
하고 있는 일을 알려준 셈이라 줄일 것이 없었다.

**② 그 여유를 내가 없앴다.** 탐색 라운드가 앱을 데워 놓았다:
```
melon    탐색 7.0턴 → 본측정 A 4.2턴   (−2.8)
storage  탐색 8.5턴 → 본측정 A 7.4턴   (−1.1)
musinsa  탐색 10.0턴 → 본측정 A 9.6턴  (−0.4)
```
데워지는 건 예상했고 *"A·B 가 같은 조건을 만나니 오히려 좋다"* 고 적었다. **그건 맞지만,
동시에 기억이 없애려던 낭비 자체를 없앤다는 걸 놓쳤다.** 기억은 **차가운** 행동을 보고
썼는데 **데워진** 행동에 대고 쟀다.

**③ storage 의 기억은 정작 필요한 순간에 안 붙었다 ★**
```
 22턴  com.android.settings          ← 기억 주입됨
 20턴  com.sec.android.app.myfiles   ← 안 붙음 (저장공간 화면)
 12턴  com.samsung.android.lool      ← 안 붙음 (디바이스 케어)
  9턴  com.android.settings.intelligence
```
**74턴 중 22턴에만 붙었다.** 디바이스 케어와 저장공간이 **별도 패키지**라, `APP_FACT` 가
`com.android.settings` 에 묶여 있으니 **에이전트가 그 기억이 가리키는 곳에 들어가는 순간
기억이 사라진다.** 실험 잡음이 아니라 **주입 설계의 버그**다(→ 아래 열린 결정).

**④ musinsa 의 기억은 애초에 도움이 될 수 없는 문장이었다 ★**
처음엔 musinsa 를 "가장 깨끗한 증거"로 적었다(기억이 96턴 중 85턴에 붙었고 결과가 정확히
±0.0%, 스크롤도 A 13 : B 13). **그 판단을 철회한다.** 두 가지가 틀렸다:

- **낭비의 종류가 달랐다.** musinsa 에서 스크롤은 *가격을 읽으려고* 한 것이다.
  **화면 밖 정보를 읽으려면 어차피 화면을 움직여야 한다** — 배치를 알려줘도 그 턴은 안
  사라진다. 즉 없앨 수 없는 턴을 없애려 한 것이다.
- **문장이 행동으로 옮겨지지 않는다.** "사진→브랜드→이름→별점→색상→가격" 은 *순서*만
  알려주고 *거리*를 안 알려준다. 모델이 이걸로 할 수 있는 게 없다.

### 한 과제는 여러 패키지를 넘나든다 — `pkg` 하나로 묶으면 기억이 사라진다 ★
**해결됨 (2026-09-16).** "저장공간 확인"의 실제 이동 경로:
```
com.android.settings              설정 본체
com.android.settings.intelligence 설정 '검색' UI      ← 별도 패키지다
com.samsung.android.lool          디바이스 케어
com.sec.android.app.myfiles       저장공간 상세
```
`APP_FACT` 가 `com.android.settings` 에만 묶여 있으니 **에이전트가 그 기억이 가리키는
곳에 들어가는 순간 기억이 사라졌다** — 74턴 중 22턴에만 붙었다.

**고친 방식**: `pkg` 가 **쉼표 목록**을 받고, `scope = "system"` 이면 패키지를 안 본다.
판정은 SQL 이 아니라 코틀린에서 한다(`MemoryGateway.appliesTo`) — PITFALL 키워드가
이미 같은 이유로 코틀린에 있다. 실측: 22/74(30%) → **6/11(55%)**.

⚠️ **`vendor` 는 구현하지 않았다.** 접두사로 제조사를 판정하려 했는데 실제 데이터가
허락하지 않는다 — 같은 삼성 기능인데 `com.android.` · `com.samsung.android.` ·
`com.sec.android.` 로 **공통 접두사가 없다.** 억지 목록을 박느니 쉼표 목록이 정직하다.

⚠️ **패키지 목록은 추측하지 말고 `episode.pkg` 를 보고 채울 것.** 설정 검색이 별도
패키지라는 건 고친 뒤 재측정해서야 알았다. 리플렉터도 같은 방법으로 자동 생성할 수 있다 —
그 실행의 episode 에 찍힌 패키지를 모으면 된다.

### 낭비에는 두 종류가 있다 — 기억은 한쪽만 줄인다 ★
판별 기준 한 줄: **그 턴을 없애도 목표를 달성할 수 있나?**

| | 뜻 | 기억이 줄이나 | 예 |
|---|---|---|---|
| **탐색형** | 어디로 가야 할지 몰라 헤맨다. 경로를 알았다면 **애초에 없었을** 턴 | ✅ | storage 의 `drag` 8회 + 잘못 짚고 `go_back` |
| **읽기형** | 정보가 화면 밖에 있어 **누군가는 화면을 움직여야** 한다 | ❌ | musinsa 의 `drag` 13회 |
| (대기) | 앱이 느린 것. 에이전트가 헤매는 게 아니다 | ❌ | melon `wait` 6, musinsa `wait` 10 |

실행을 이 기준으로 나눠 보면 여유가 어디 있는지 바로 보인다:

| | 고정비(`list_apps`+`open_app`) | **줄일 수 있는 것** | 못 줄이는 것 |
|---|---|---|---|
| melon 21턴 | 10 | **0** | `click` 5(재생) · `wait` 6 |
| storage 37턴 | 10 | **8~10 (탐색형)** | 진짜 이동 `click` · `type` 2 |
| musinsa 48턴 | 10 | 3~5 | `drag` 13(읽기) · `wait` 10 |

**melon 은 줄일 수 있는 턴이 0이다** — 실험에 넣은 것 자체가 실수였다.
**다음 과제의 기준: 탐색형 낭비가 전체의 20% 이상.** 세 과제 중 storage(~25%)만 만족한다.

⚠️ 대기(`wait`)를 기억으로 줄이려 하지 말 것 — "이 앱은 빠르니 기다리지 마라"는 **지시문**이고,
틀리면 모델이 빈 화면을 보고 판단한다(아래 '서술문으로 쓴다' 규칙 위반).

### 그래서 이 측정이 실제로 잰 것
| 과제 | 왜 결론을 못 싣나 |
|---|---|
| melon | 잴 여유가 없었다 (바닥 효과) |
| storage | 기억이 필요한 곳에 **안 붙었다** (주입 버그) |
| musinsa | 기억이 **행동으로 옮길 수 없는** 문장이었고, 낭비도 **읽기형**이었다 |

> **사전 기준으로 "효과 없음"이 맞고 그 판정은 유지한다.**
> 다만 이 실험은 *"앱 UI 지도 급 지식"* 이 아니라 **"내가 쓴 세 문장"** 을 잰 것에 가깝다.

### 이것이 말해주지 *않는* 것
- "기억 시스템이 쓸모없다" ← 잰 것은 **1급(앱 UI 지도)** 하나뿐이다
- "리플렉터가 실패한다" ← 리플렉터는 존재하지도 않는다
- **세 과제 전부** 각각 다른 이유로 오염됐다 — 깨끗한 음성 결과는 **하나도 없다**

## Unit 4 — 측정 계획 (2026-09-16 확정)

### 두 판단을 헷갈리지 말 것
| | 질문 | 설계 | 언제 |
|---|---|---|---|
| **판단 ①** | 사람이 써 준 기억을 **주입**하면 효과가 있나 | A/B | **Unit 4** |
| **판단 ②** | 에이전트가 스스로 **쌓은** 기억이 시간이 갈수록 도움이 되나 | 종단(longitudinal) | **Unit 5 뒤** (6→5 순서 변경 반영) |

**A/B 로는 ②를 원리적으로 못 잰다** — 두 조건을 나란히 놓는 설계라 시간에 따른 변화를
볼 모양이 아니다. ①이 거짓이면 ②는 물어볼 필요가 없어서 이 순서다.

### 확정된 설계
| | |
|---|---|
| 과제 1 | `멜론에서 이문세의 붉은 노을 틀어줘` |
| 과제 2 | `저장공간이 얼마나 남았는지 알려줘` |
| 과제 3 | `무신사에서 윈드브레이커 검색해서 첫 번째 상품 이름이랑 가격 알려줘` |
| 조건 A / B | 기억 0건 / 사람이 쓴 기억(탐색 라운드에서 작성) |
| 모델 | `gemini-3.5-flash` / `low` — 30회 고정 |
| 최대 턴 | 20 (소켓 `RUN` 고정값) |
| 반복 | 과제당 A 5 + B 5, **번갈아** (`A B A B …`) |
| 지표 | `run.outcome` · `run.turnsUsed` |

**판정 기준 — 숫자를 보기 전에 정했다. 결과를 보고 바꾸지 않는다:**
> 평균 턴 수가 **20% 이상** 줄고, **3과제 중 2과제 이상**에서 방향이 같으면 "효과 있다"

**제외할 실행:** `memoryReadFailed=1` · A인데 `memoryCount≠0` · `model` 불일치 ·
**`run` 행이 30개가 아님**(DB가 죽으면 행 자체가 없다)

### 왜 번갈아 돌리나
A→A→A→B→B→B 로 돌리면 시간대·네트워크·앱 학습이 조건과 엮인다. 특히 **앱이 스스로
학습한다** — 무신사 최근 검색어, 멜론 최근 재생이 회차마다 쌓여 뒤로 갈수록 쉬워진다.
번갈아 돌리면 그게 A·B 에 고르게 퍼져 **편향은 안 생기고 신호만 약해진다.**

이 요구 때문에 소켓 명령이 필요했다 — 손으로 목록 UI 를 만져야 하면 번갈아 돌릴 수 없다.

### 지식의 '급' — 결과를 어떻게 읽을 것인가 ★
기억에도 급이 있고, **이번에 재는 문장 셋은 전부 가장 낮은 급**이다.

| 급 | 예 | 몇 개나 필요한가 |
|---|---|---|
| **1. 앱 UI 지도** | "디바이스 케어 → 저장공간" | **앱 × 화면 수** ← 이번 측정이 여기 |
| 2. 앱의 버릇 | "이 앱은 스플래시가 뜬다" | 앱당 한둘 |
| 3. 제조사·업계 패턴 | "삼성은 기기 관리를 디바이스 케어에 모은다" | 훨씬 적고 오래간다 |
| 4. 사용자 사실 | "이 사람은 음악을 멜론으로 듣는다" | 범용 모델이 알 수 없는 것 |

1급은 개수가 폭발하고, 앱 업데이트에 제일 먼저 낡고, **"앱별 스크립트로 회귀"** 비판에
제일 취약하다. 1급을 고른 이유는 탐색 라운드에서 낭비가 거기서 났기 때문인데,
결과적으로 **가장 방어하기 어려운 표본**을 고른 셈이다.

**그래서 결과는 이렇게 읽는다:**
> ❌ "기억이 효과가 있다/없다"
> ✅ **"앱 UI 지도 급 지식이 값어치를 하는가"**

- **효과 없음** → 리플렉터를 만들 이유가 없다. 기억의 종류부터 다시 생각한다.
- **효과 있음** → 그래도 *"앱 지도를 쌓자"* 는 결론이 **아니다.** 다음 질문은
  **"같은 효과를 2·3급 문장으로 낼 수 있나"** — 개수가 훨씬 적다. 그게 Unit 5 설계를
  가르고, `MemoryEntity.scope`(`pkg|vendor|system`)를 실제로 쓰기 시작하는 지점이다.
  **지금은 칸만 있고 `pkg` 말고 쓰는 코드가 없다.**

자기강화 위험을 적어 둔다 — 1급이 효과 있다고 나오면 1급을 뽑는 리플렉터를 만들게 되고,
끝에는 앱 지도를 갖게 된다. 측정은 "성공"했는데 방향은 그쪽으로 간다.

### 기억 문장은 서술문으로 쓴다 — 지시문은 위험하다 ★
첫 초안의 멜론 문장이 이랬다:
> *Recently played songs appear on the home screen — tap one **instead of searching**.*

**요청한 곡이 최근 목록에 없으면 엉뚱한 곡을 튼다.** 확인 단계를 건너뛰라고 **지시**하는
기억은 실패를 만들고, 그 실패는 "기억이 오히려 해로웠다"로 집계된다.

고친 형태는 사실만 말하고 판단은 모델에게 남긴다:
> *The home screen lists recently played songs; **a song already there** can be played
> without searching.*

**규칙: 기억은 "이렇게 해라"가 아니라 "이렇게 생겼다"로 쓴다.** 명세서 §2 의 리플렉터
출력 템플릿(`trigger` → `consequence`)이 이걸 구조적으로 강제하는 이유이기도 하다.

### 미리 아는 한계 — 결과에 같이 적을 것
1. **앱의 자체 학습** (위)
2. **기억은 답을 알고 쓴 문장이다.** 그래서 ①이 재는 건 "기억이 도움이 되나"보다
   **"사람이 완벽히 써 줬을 때의 상한"** 에 가깝다. 리플렉터는 이보다 잘할 수 없다.
3. **앱이 스스로 학습한다 — 탐색 라운드가 '데우는' 역할을 했다.** 멜론 탐색 1회차는
   9턴, 2회차는 **5턴**이었다. 1회차가 곡을 틀어 '최근 들은 곡'에 올려놨기 때문이다.
   무신사도 최근 검색어가 남아 2회차는 랜딩 화면을 건너뛰었다. 본 측정은 처음부터
   데워진 상태로 시작하므로 A·B 가 같은 조건을 만난다 — **앱 데이터를 지우지 말 것.**
4. **정확도는 안 잰다.** 지표가 턴 수라 모델이 가격을 틀리게 말해도 `success` 다 —
   나중에 "정확도가 좋아졌다"고 쓰지 말 것.

### 순서
| 단계 | 폰 점유 |
|---|---|
| 1. 소켓 명령 + `confirm()` 수정 | ✅ **완료** |
| 2. 러너 `tools/bench_memory.py` | ✅ **완료** — 1회 전 구간 검증(8턴 87초) |
| 3. 아래 '측정 전 준비물' (손으로) | — |
| 4. 탐색 라운드 6회 → 기억 문장 작성 → **사람이 확인** | ~10분 |
| 5. 본 측정 30회 | **~40분** |
| 6. 집계·판정 | — |

4단계에서 멈추고 기억 문장을 사람에게 보인다. **그 문장이 곧 실험의 처치**라, 혼자 쓰고
혼자 재면 결과가 나빠도 "기억이 안 통한다"인지 "문장을 못 썼다"인지 구분할 수 없다.

### 측정 전 준비물 — 하나라도 빠지면 배치가 통째로 날아간다
```bash
adb shell dumpsys deviceidle whitelist +com.cua.a11    # ★ 안 하면 앱이 얼어 소켓이 죽는다
```
| | 왜 |
|---|---|
| **배터리 최적화 제외** (위 명령) | 백그라운드 방치 1분이면 앱이 얼어붙는다. 증상이 '서버가 죽었다'와 똑같아 찾는 데 오래 걸린다(`CLAUDE.md` Gotchas). **재부팅하면 풀린다** |
| 앱에서 모델을 **3.5-flash / low** 로 | 소켓 `RUN` 은 앱이 마지막에 고른 설정을 물려받는다. 앱 재시작 직후엔 기본값(3.5/low)이지만, 앱에서 한 번이라도 `실행` 을 누르면 저장값(지금 3.8/medium)으로 덮인다 |
| 화면 켜고 **잠금 해제** | 꺼져 있으면 0.4초에 실패 |
| 충전기 | 40분 + 배터리가 빠지면 절전 모드가 켜져 동작이 달라진다 |
| PC·폰 **같은 네트워크**, 배치 중 **Wi-Fi 옮기지 말 것** | 소켓이 Wi-Fi 로 간다 |

### 소켓 측정 명령 (Unit 4 에서 추가)
| 명령 | 응답 |
|---|---|
| `MEMCLEAR` | `OK <지운 수>` |
| `MEMADD <json>` | `OK <id>` 또는 `ERR <사유>` |
| `MEMCOUNT` | `OK <건수>` |
| `DUMP` | 마지막 run + episodes JSON 한 줄 |

`MEMADD` 의 JSON: `{"kind","text","pkg"|"keywords"}` (+ 선택 `state`·`sensitivity`).
`source` 는 **`bench`** 로 박힌다 — 사람이 넣은 `user_ui` 와 섞이면 측정 후 무엇을
지울지 알 수 없다. 목록 UI 에서 바로 구분된다.

⚠️ **`MEMADD` 는 반드시 `MemoryGateway.save()` 를 지난다.** DAO 를 직접 부르면
`validate()` 를 우회해 *읽기에 절대 안 걸리는 기억*이 들어가고, 그러면 **조건 B 가
사실상 A 가 되어 "기억은 효과가 없다"는 틀린 결론**이 난다.

⚠️ **측정 명령은 실패해도 반드시 한 줄을 돌려준다**(`ERR …`). 예외를 그냥 튀우면
바깥 catch 가 연결을 닫아 PC 는 빈 응답만 보고, 러너가 "거절됐다"와 "연결이 죽었다"를
구분하지 못한다. 실제로 첫 구현에서 이 버그가 났다.

---

## 종류 체계 다시 보기 — 절차 지식이 `APP_FACT` 에 얹혀 있다 (2026-09-16) ★★

### 이름이 내용을 속이고 있다
AI 에이전트 문맥의 정의로 보면:

| | 묻는 것 | 형태 |
|---|---|---|
| **의미 기억** | **무엇을 아는가** (사실·개념) | 지식 베이스 |
| **절차 기억** | **어떻게 행동하는가** (워크플로우·조작 절차) | 단계별 지침(`skill.md` 등) |

⚠️ **절차 기억이 '말로 못 옮기는 것'이라는 심리학(Tulving) 기준을 여기 끌어오면 안 된다.**
이 문맥의 절차 기억은 **글로 적힌 지침**이다. 그 기준으로 보면
`"메뉴 → 메가선생님 → 과목 → 선생님 → 커리큘럼 탭"` 은 명백히 **조작 절차 = 절차 기억**이고,
지금 `APP_FACT`(의미 기억 칸)에 앉아 있는 것은 **분류가 어긋난 상태**다.

### 그리고 실측이 이 경계와 맞아떨어진다 ★
템플릿 측정(위)을 이 축으로 다시 읽으면:

| 조건 | 무엇을 준 것 | 결과 |
|---|---|---|
| D | *"검색 결과엔 커리큘럼이 없다"* — **무엇을 아는가** | **0%** |
| B·C | *"어디를 눌러 어디로 간다"* — **어떻게 행동하는가** | **−47% / −38%** |

`방향이 있는가` 라고 불렀던 축이 실은 **의미/절차 경계**였을 수 있다. 지금까지 잰 것을
전부 이 기준으로 세우면 **의미형은 0/3**(musinsa 순서 ±0.0% · meta 위치 주장 해로움 ·
D 부정 사실 0%), **절차형만 효과를 냈다**.

**왜 그런지도 설명이 된다** — 모델은 **화면을 보고 있다.** *"무엇이 있는가"* 는 이미 알고,
모르는 것은 *"여기서 어디로"* 다. 의미 기억은 모델이 스크린샷에서 이미 얻는 것과 겹치고,
절차 지식은 **화면에 없는 것**(다음 화면)을 준다.

→ 그래서 `APP_FACT` 가 값어치를 가지려면 **화면에 안 보이는 성질**이어야 한다:
시간(`이 버튼은 3초 뒤에 반응한다`) · 화면 밖(`목록은 아래로 더 있다`) · 상태(`로그인해야 보인다`).

### 빈 칸이 하나 있다
분류는 **검색 키 × 내용**의 두 축인데 이름이 셋뿐이다:

| 검색 키 ＼ 내용 | 의미 | 절차 |
|---|---|---|
| **패키지** | `APP_FACT` | **← 이름 없음. mega 경로가 여기 얹혀 있다** |
| **목표** | `PITFALL` | — |
| **목표 + 패키지** | — | `RECIPE` |

### 그래서 지금 할 일 / 안 할 일
- **지금 옮기지 않는다.** 기억이 몇 건이라 검색 키 차이가 드러나지 않고, 옮기려면 읽는
  쪽(Unit 9)을 먼저 만들어야 한다.
- **`RECIPE` 의 '장치' 는 여전히 불필요해 보인다** — 경로를 `APP_FACT` 한 줄로 넣어도
  −38% 였다. `steps` 배열·`precondition`·압축이 없어도 된다.
- **그러나 `RECIPE` 의 '검색 키' 는 필요해질 것이다.** `APP_FACT` 는 그 앱에 있기만 하면
  **목표와 무관하게** 붙는다. *"커리큘럼 가는 길"* 은 강의를 재생하려는 작업에서 예산
  한 줄만 까먹는다. 앱마다 경로가 여러 개 쌓이면 **예산 2줄을 두고 경쟁**한다.
- ⚠️ **"Unit 9 는 선택" 이라는 판단을 다시 봐야 한다.** 효과를 낸 것이 절차 지식인데
  그 범주를 선택 사항으로 둔 것은 앞뒤가 안 맞는다. 다만 필요한 것은 **장치가 아니라
  검색 키**다 — 그 형태로 다시 설계할 것.

---

## 경로 지식은 APP_FACT 인가 RECIPE 인가 — Unit 9 의 판단이 여기 달렸다 ★

메가스터디 기억("커리큘럼은 메뉴 → 메가선생님 → 과목 → 선생님 → 커리큘럼 탭")은 **내용상
경로**라 RECIPE 처럼 보인다. 하지만 명세서 부록 C 는 **내용이 아니라 검색 키와 주입 경로로**
분류한다:

| 종류 | 검색 키 | 주입 경로 |
|---|---|---|
| `APP_FACT` | 패키지명 | `appNote` · **매 턴** |
| `PITFALL` | 목표 키워드 | `taskNote` · 실행 내내 |
| `RECIPE` | 목표 + 패키지 | `taskNote` · **precondition 확인 후** |

패키지로 찾아 매 턴 붙으니 **기계적으로 `APP_FACT` 다.** 판정 순서("특정 앱에서만 참인가
→ APP_FACT")로도 같다.

**둘의 진짜 차이는 판정해야 할 것이다** — `APP_FACT` 는 "참인가" 하나, `RECIPE` 는
**"참인가" + "최단인가"** 까지. 명세서 표현으로 *"성공한 20턴이 본질 12턴 + 헤맨 8턴일 수
있고, 사후에 그걸 가려내는 게 판단"* 이다. **메가스터디가 정확히 그 상황이다**(16~20턴 중
본질 ~8턴 + 검색에서 헤맨 4~10턴). 지금 모으는 턴 수가 곧 `RECIPE.observedTurns` 다.

### 그래서 이번 측정이 Unit 9 에도 답을 준다
| 결과 | 함의 |
|---|---|
| 경로를 **글로** 알려주니 턴이 줄었다 | `RECIPE` 의 추가 장치(`steps`·`precondition`·압축)가 **없어도 된다.** 한 줄 서술로 충분 |
| 경로는 맞는데 글로는 안 줄었다 | 절감이 **"모델의 판단 턴을 건너뛰는 것"** 에서만 나온다 → 그게 `RECIPE` 영역 |

⚠️ 아래쪽이 나오면 **위험도 같이 온다.** 턴을 건너뛴다는 건 화면을 안 보고 행동한다는
뜻이라, 설계 원칙 1이 금지한 좌표 매크로와 **같은 방식으로 실패한다.** 지금 형태는 모델이
매 턴 화면을 보고 판단하고, 문장이 틀리면 화면을 보고 무시한다 — 그 안전성을 유지한 채
경로 지식만 주는 형태다. `RECIPE` 로 넘어가려면 그 안전성을 무엇으로 대신할지 먼저 답해야 한다.

---

## Unit 5 를 켜기 전에 반드시 — 내용 필터 ★

명세서 §3 의 **규칙 B**("화면의 구조는 저장하고 화면의 내용은 저장하지 않는다")를 집행하는
코드가 **아직 없다.** `MemoryGateway.validate()` 는 형식만 본다(빈 내용·검색 키 유무).

§3 이 B층(코드·결정론적)에 명시한 차단 대상: **통화 · 숫자 4자리 이상 · 전화번호 ·
이메일 · 원문 대조.** 지금은 사람만 쓰니 문제가 없지만, **리플렉터를 켜는 순간
첫 실행부터 가격·잔액·인증번호가 들어온다.** 순서를 지킬 것 — 필터가 먼저다.

판별 기준 한 줄: **다른 사람 폰에서도 참인가?** 참이면 구조, 그 사람에게만 참이면 내용.

---

## 다음 할 일

**Unit 5 — 리플렉터.** Unit 6 의 승격 카운터는 실기기 검증까지 끝났다(위 Unit 6 절).
남은 reconciliation 은 **후보를 만드는 리플렉터가 있어야 입력이 생기므로** 5 와 함께 간다 —
6 을 앞당긴 목적(검증층을 자동 쓰기보다 먼저)은 `PENDING` 검역과 점수 강등이 서 있는 것으로
이미 달성됐다.

**시작 전에 반드시** — §3 규칙 B 의 **내용 필터**가 먼저다(위 'Unit 5 를 켜기 전에' 절).
그리고 리플렉터는 **로그만 본다**는 점을 설계에서 정면으로 다뤄야 한다(아래).

아래는 이 순서를 정한 근거라 그대로 둔다. Unit 4 는 2차까지 끝났다(판단 ① = 조건부).
원래 순서는 5(리플렉터) → 6 이었는데 **바꿨다** — 2차에서 **틀린 기억이 에이전트를 적극적으로
망가뜨린다**는 게 나왔다(meta +26.7%, 없는 것을 네 번 스크롤). 검증층 없이 자동 쓰기를 켜면
틀린 기억이 첫 실행부터 주입된다. 삭제 UI(Unit 3)를 자동 쓰기보다 먼저 만든 것과 같은 논리다.

### 만들 것 — 두 덩어리인데 **처지가 다르다** ★
| | 무엇 | 지금 입력이 있나 |
|---|---|---|
| **승격 카운터** | 주입된 실행이 성공 `score+1`(상한 4) · 실패 `score−1` · `score≥2` → PENDING→ACTIVE · `score≤0` → RETIRED | ✅ 사람이 넣은 기억 + `run.outcome` 만으로 돈다 |
| **reconciliation** | 후보 × 기존 기억을 모델에 넘겨 `ADD`/`NOOP`/`UPDATE`/`DELETE` 판정. **`NOOP` 이 곧 `score+1`** | ❌ 후보를 만드는 리플렉터가 Unit 5 다 |

⚠️ **"6 을 끝냈다"의 의미가 두 갈래다.** 승격 카운터는 오늘 실기기에서 끝까지 검증된다.
reconciliation 은 **손으로 만든 후보**로만 시험할 수 있고 진짜 입력은 Unit 5 가 와야 생긴다.
계획에서 뭉뚱그리면 나중에 "검증했다"고 잘못 적게 된다.

### 시작 전에 알아야 할 것
- **칸은 이미 다 있다** — `state`·`score`·`pinned`·`supersededBy`·`invalidAt`·`sourceRunId`
  전부 스키마에 있고 **쓰는 코드만 없다**.
- ⚠️ **`run` 과 `memory` 를 잇는 고리가 없다 — 이게 Unit 6 의 첫 일이다.** 승격 카운터는
  *"이 실행에 무엇이 주입됐나"* 를 알아야 하는데 지금 남는 건 `episode.note` 의 **문장 원문**
  뿐이다. **문자열로 역매칭하지 말 것** — 예산 때문에 `taskNote`+`appNote` 가 합쳐져 나가고,
  사람이 문장을 고치면 곧바로 끊긴다. 주입 시점에 id 를 잡아야 한다.

  **먼저 정할 것 — 그 고리를 어디에 두나.** 둘 다 성립하고 대가가 다르다:

  | | 무엇 | 대가 |
  |---|---|---|
  | **RAM 전용** | `MemoryGateway` 가 실행 중 emit 한 id 를 들고 있다가 `onRunEnd` 에 점수를 매긴다 | **마이그레이션 0.** 대신 **사후 감사가 안 된다** — "이 기억이 어느 실행에서 점수를 받았나"를 나중에 못 되짚는다 |
  | **DB 에 기록** | `episode` 에 칸을 더하거나 `(runId, memoryId)` 표를 새로 판다 | 마이그레이션 1회 + 위의 '스키마를 바꾸면 조용히 죽는다' 점검 |

  프로세스가 죽으면 그 실행은 `outcome` 도 없다 — **내구성 때문에 DB 가 필요한 건 아니다.**
  갈림길은 내구성이 아니라 **감사 가능성**이다. Unit 4 에서 "기억이 정말 주입됐나"를
  `episode.note` 로 확인해야 했던 걸 생각하면, 같은 질문이 점수에도 온다.
- ⚠️ **지금 있는 기억은 대부분 카운터가 안 건드린다.** 목록 UI 로 넣으면 `pinned` 가 기본
  체크(자동 강등 면제)라, 실제로 점수가 움직이는 건 `source=bench` 로 들어온 것뿐이다.
  검증 시나리오를 짤 때 이걸 모르면 "카운터가 안 돈다"고 오진한다.
- `score`·`pinned`·`무효화됨` 은 목록·편집 화면에 **이미 보인다**. 새 상태를 만들면
  거기도 같이 보여야 한다 — 사용자가 ACTIVE 인데 왜 안 나오는지 알 수 있어야 한다.

### 그다음
| 순서 | | 전제 |
|---|---|---|
| **5** | 리플렉터 (추출 + §3 필터) | **§3 규칙 B 의 내용 필터가 먼저다** — 위 'Unit 5 를 켜기 전에' 절. `validate()` 는 형식만 본다 |
| **판단 ②** | 스스로 쌓은 기억이 시간이 갈수록 도움이 되나 (종단) | 5 가 있어야 성립한다 — 자동으로 쌓인 기억이 없으면 잴 것이 없다 |
| 7 · 8 | 랭킹 · 생애주기 | **8 까지가 완성.** 없으면 낡은 기억이 계속 주입돼 시간이 갈수록 나빠진다 |

### Unit 4 재측정 항목은 전부 반영됐다
1차 뒤에 적어둔 네 가지(주입 버그 `scope` 수정 · 탐색형 낭비 20% 이상인 과제로 교체 ·
데워진 뒤 관측 · 행동으로 옮길 수 있는 문장)는 2차에서 다 했다. **대신 새 규칙이 하나 나왔다** —
*기억 문장의 근거는 로그가 아니라 화면이다.* meta 의 +27% 가 정확히 이걸 어겨서 났다.

⚠️ **이 규칙이 리플렉터에 그대로 걸린다 — 리플렉터는 로그만 본다.** 사람이 로그만 보고
쓴 문장이 틀렸는데, 같은 재료로 기계가 쓰면 더 나을 이유가 없다. Unit 5 설계에서 답해야 할
질문이다(후보의 근거를 화면에서 어떻게 확보할 것인가), 그리고 그 답이 없는 동안
**Unit 6 의 검역(PENDING)이 유일한 방어선**이다.
